# 다음 작업 프롬프트 — 주사위 골목

> 새 세션에 이 파일을 참조로 붙여 이어가면 됩니다. `CLAUDE.md`는 세션 시작 시 자동 로드(아키텍처·함정).

---

## 현재 상태 (2026-07-22 기준)

- 위치: **`~/yacht-dice`** 가 유일한 저장소. 버전 **v1.162.0**.
- ⚠️ **디렉토리 정리됨**: 예전에 있던 `~/Desktop/yacht-dice`(낡은 사본)는 **이번 세션에 삭제**했다. 이제 `~/yacht-dice` 하나뿐. (과거 핸드오프의 "Desktop 사본 주의"는 무효)
- 미커밋: `scripts/online-test.js`(사용자 것) — **건드리지 말 것.**

## 2026-07-22 세션 (v1.161) — 윷 대전 방식 3택 (일반·스피드전·아이템전)

기존엔 **아이템전만 토글**이었고 **스피드전은 '오늘의 규칙' 랜덤**으로만 나왔다. 셋업에서 고르게 통합.

- 엔진: `opt.speedStart` 추가 — `_applyDailyStart()` 조건을 `dailyRule==='speed' || speedStart`로. serialize에도 노출.
- 서버: create 옵션·lobby 페이로드·startEngine에 `speedStart` 전달.
- 클라: `cfg.battleMode`('normal'|'speed'|'item', `yut_bmode`)로 통합. **예전 `yut_items='1'`는 자동으로 item으로 이어받는다.** 로컬/온라인 셋업 모두 3택 칩, 로비 규칙 요약에도 모드 표시.
- **오늘의 규칙에서 `speed`는 뺐다(v1.162)** — 모드로 직접 고르게 된 뒤엔 '일반인데 말이 먼저 나와 있는' 충돌이 생겨서다. 남은 규칙: `catchfest`·`bigbackdo`·`goldrain`(+ 절반은 규칙 없음). 클라의 `speed` 뱃지 정의는 남겨뒀다(옛 판 재현·되돌리기 대비).
- 검증: 엔진 단위(일반 0개 / 스피드 편당 1개 node3 / 아이템 2장) · 3모드 × 12판 풀시뮬 정상 종료 · 로컬 UI·온라인 서버 경유 e2e.

## 2026-07-22 세션 (v1.160) — 윷 '어디로 갈지' 고르기 (초보 혼란 해소)

**제보**: 모/윷+도처럼 끗수가 여러 개 남았을 때, 도부터 쓰려면 **칩을 먼저 누르고 말을 눌러야** 하는 걸 처음 하는 사람은 모른다. 그냥 말을 누르면 목록 첫 끗수로 움직여 버린다.

- **말 → 목적지** 순서로 바꿨다. 말을 누르면 갈 수 있는 칸에 고스트 + 끗수 라벨(`모 5칸`, `도 1칸`, 완주면 `🏁`)이 뜨고 그 칸을 눌러 고른다. 같은 말을 다시 누르면 취소.
- **한 번에 가던 경우는 그대로**: 갈 곳이 하나거나(끗수 1개·도착 칸이 같음) 칩을 미리 골라뒀으면 예전처럼 즉시 이동 — 익숙한 사람 손해 없음.
- 구현: `showStepGhosts`/`clearStepGhosts`/`performPieceMove`(yut.html, 마커 밖). 고스트는 `#pieceLayer`에 SVG로 그리고 `.stepghost`는 `pointer-events:auto` + 반경 22px 히트 영역(모바일 탭). 맨 윗줄 칸은 라벨이 판 밖으로 나가서 아래로 뒤집는다.
- 안내문/튜토리얼 STEP 2 문구도 새 흐름으로 갱신.
- 검증: 데스크톱·모바일(390) 각각 모+도/윷+도 상황에서 **고른 끗수만 소비**되고 나머지가 남는 것 확인.

## 2026-07-22 세션 (v1.159) — 방 나가기/파하기 + 재입장 결과 재현 버그

**사용자 제보**: 온라인 판이 끝난 결과 화면에서 방을 못 끝내고, 골목으로 나갔다 윷놀이로 다시 들어가면 "내가 계속 1등한 걸로 판정"됨.

| 문제 | 원인 | 고침 |
|------|------|------|
| 재입장하면 지난 판 결과창이 다시 뜨고 전적이 또 쌓임 | 윷의 결과창 '골목으로'가 `$('toHub').onclick=()=>location.href='./'` 뿐이라 **자리 정보(yut_room/pid)를 안 지웠다** → 재입장 시 끝난 판으로 자동 rejoin → showResult 재실행 → `Stats.record` 재호출. **5종 중 윷만** 이랬다(kb·ld·lcr·yacht는 이미 정리 중) | ① 골목으로에서 `teardownNet()`+`seatDel()` ② **판 고유키 가드**: `방코드:gameStartTime`을 `*_lastres`에 저장해 같은 판은 한 번만 기록(메모리 플래그는 새로고침을 못 견딤) |
| 결과창에서 대기실로 못 감 | 윷·lcr엔 버튼 자체가 없었고, 요트는 **호스트 전용**이었다(서버는 판 종료 후 전원 허용) | 5종 전부 결과창에 '대기실(로비)로' — 온라인이면 누구나. 서버 `endGame`은 종료 상태에서 전원 허용 |
| 방을 파할 수 없음 | 기능 자체가 없었다 | 서버 `{t:'closeRoom'}`(호스트만) → `roomClosed` 브로드캐스트 후 `destroyRoom`. 클라는 자리 정보까지 지우고 셋업으로. 버튼은 **대기실 + 결과창**(호스트만, 확인창) |

- ⚠️ `roomClosed` 알림은 **destroyRoom 전에** 보내야 한다(파괴가 소켓 meta를 비워서 이후 브로드캐스트가 안 간다).
- 엔진 4종(kb/ld/lcr/yacht)에 `gameStartTime` 추가(윷엔 이미 있었음) — 판 고유키용. serialize에 포함.
- 검증: 5종 방 파하기 e2e(호스트만 보임·게스트 튕김·양쪽 자리 삭제·콘솔 0) · 골목으로 자리 삭제 · 가드 로직(같은 판 1회/다른 판 재기록) · test:online · test:online2p.

## 2026-07-22 세션 (v1.158) — 빽도 완주 규칙 (사용자 요청)

**규칙**: 빽도로 물러나 출발점 자리에 선 말은 **다음 도~모 아무거나로 바로 완주**한다.

- 구현 열쇠: 이 엔진엔 출발점(0)과 **완주 게이트(29)**가 따로 있고 **같은 좌표에 렌더**된다(`nodeXY: 29→0`). 그래서 빽도로 물러난 말을 0이 아니라 **29에 세우면** 기존 전진 로직이 그대로 완주로 이어진다 — 별도 분기 없음.
- 적용 지점: `step()` 미출전 빽도 / `step()` 후퇴 시 outer의 0 → 29, `buildPath()` 동일(경로 마지막 칸 ≡ step 최종 보장), 클라 `simStep`(yut.html, 마커 밖) 동일.
- ⚠️ **빽도(-1)에만 적용한다.** 처음엔 후퇴 이벤트(-2)까지 29로 보내서 '뒤로 2칸' 페널티가 완주 직전 보너스로 뒤집혔다(시뮬에서 잡음). 조건에 `steps === -1`이 있는 이유.
- 첫 칸(1)에서 빽도로 물러나는 경우도 같은 규칙(정통 규칙과 일치).
- UI: 규칙 모달 문구 갱신 + 게이트에 말이 있고 전진 끗수가 남아 있으면 턴 안내에 "🏁 출발점 말은 도~모 아무거나로 바로 완주!" 힌트.
- 검증: 80판 시뮬(이동 3671회) — 빽도 첫진입 66회 전부 29, 게이트 전진 152회 전부 완주, `buildPath`↔`step` 불일치 0. 서버 경유 온라인 e2e로도 진입→완주 확인.

## 2026-07-22 세션 (v1.157) — 남은 UX 후보 3건 마무리

| 증상 | 고침 |
|------|------|
| 윷 **데스크톱 판이 400px 고정**이라 화면 아래 1/3이 빈 공간 | ≥1100×760에서 판을 `min(600px, 100vh-330px)`로 키우고(1440×900 기준 400→570px) `body.ingame`을 flex로 만들어 `.app`에 `margin:auto` 세로 중앙. margin:auto라 내용이 넘치면 0으로 무너져 위가 안 잘린다 |
| 윷 **튜토리얼 코치마크가 결과·안내를 덮음**(모바일) | 팁 위치 규칙을 '아래 공간 200px 넘으면 아래' → **후보 4곳(대상 위/아래·화면 위끝/아래끝) 중 중요 UI를 가장 덜 가리는 곳** 선택으로 교체. 스포트라이트 대상은 절대 안 가리게 우선 필터. 모바일 팁은 컴팩트(264px) |
| 너클본즈 **첫 규칙 모달이 잘림** | `.sheet.guide`는 84vh 스크롤이었는데 '알겠어!'가 스크롤 끝(897px)에 있어 끝까지 내려야 닫혔다 → 버튼을 시트 하단 sticky로. 같은 문제가 있던 **요트 규칙 오버레이**도 함께(`.result`에 max-height+스크롤, `.btns` sticky) |

> 요트 `.result`는 결과 화면과 공용이라 스크롤 허용이 결과 화면에도 이득이다. 짧은 폰(667px)에서 위아래가 잘리던 것도 같이 해결.

## 2026-07-22 세션 (v1.156) — 모바일 UX 3건 (직접 플레이해보고 잡은 것)

5종을 실제로 굴려보고 데스크톱(1440×900)·모바일(390×844) 화면을 눈으로 본 뒤 고친 것들. 캡처는 세션 스크래치패드에 있었다(휘발).

| 증상 | 원인 | 고침 |
|------|------|------|
| 모바일 인게임 **제목이 세로로 쪼개짐**(윷 3.4줄·요트 3.1줄, 헤더 40→62px) | 인게임에서 상단바 버튼이 늘어나 flex 텍스트가 글자 단위로 줄바꿈 | 5종 `.title/h2`에 `white-space:nowrap;min-width:0;overflow:hidden` + ≤480px 상단바 압축(아이콘 36px, 모드칩 축소) |
| **LCR AI 말풍선이 헤더를 덮고 서로 겹침** | `floatReaction`이 카드 위(`top-8`)에 놓고 위로 110px 떠오르는데, 모바일은 카드가 상단바 바로 밑 | 상단바 아래로 하한(`bar.bottom+8+EMO_RISE`) + 동시 말풍선은 42px씩 계단 배치(`liveEmos`) |
| **너클본즈 모바일에서 내 판이 화면 밖**(문서 1185px vs 화면 844px) → 매 턴 스크롤 | 상대판·주사위·내판이 세로로 쌓이는데 압축이 없었음 | ≤480px 세로 압축(상대판 작게, 내 판은 크게 유지) + ≤800px 높이에선 한 단계 더. **주의: 이 미디어 블록은 스타일 맨 끝에 둬야 한다** — 앞에 두면 뒤의 `.tray/.arena/.side` 기본 규칙에 덮인다(실제로 덮였다) |

> 시도했다 되돌린 것: 굴리기 버튼 `position:sticky` — 짧은 화면에서 **내 판의 탭 영역을 가려** 더 나빴다. 높이 기준 압축으로 대체.

## 2026-07-22 세션 (v1.154~155) — 채팅 2인 실측 + 자리 소유권 버그

대기 목록의 "채팅 2인 실측"을 실제 크로미움 탭 2개로 돌렸다. **채팅·재접속은 전부 정상**(즉시 표시·수신·연타 5개 무손실·순서·IME Enter 오전송 없음·중복 없음).
대신 그 과정에서 **진짜 버그**가 나왔다:

- **증상**: 같은 브라우저에서 두 탭을 열면 뒤에 join한 탭이 `*_room`/`*_pid`(localStorage)를 덮어써서, 먼저 있던 탭이 새로고침(=모바일 화면잠금 복귀)하면 **남의 자리로 rejoin** → 서버가 옛 소켓을 닫고 두 탭이 서로 끊는 루프. 채팅·상태가 통째로 안 온다.
- **고침(v1.154)**: 5종 모두 자리 정보를 **sessionStorage(탭 전용) 우선**으로 읽고 쓴다. `localStorage`에도 함께 남겨 허브 '이어하기' 배너는 그대로 동작. 요트는 `SEAT.get/set/del`(JSON 래퍼 LS 위), 나머지 4종은 호이스팅되는 `seatGet/seatSet/seatDel` 함수.
- **회귀 테스트 신설**: `npm run test:online2p` (= `scripts/browser-test/verify-online.js`) — 탭 2개로 요트 채팅 왕복 + 5종 자리 격리/새로고침 rejoin. **일부러 컨텍스트를 격리하지 않는다**(그 버그를 잡는 게 목적).
- **새 탭 폴백까지 막음(v1.155)**: 초대 링크를 같은 브라우저 새 탭으로 열면 sessionStorage가 비어 localStorage로 폴백해 여전히 자리를 뺏었다. 부트에서 `BroadcastChannel('alley_seat')`로 "이 자리 쓰는 탭 있냐"를 묻고(250ms 타임아웃) 있으면 자동 rejoin을 포기한다. 5종 모두 `SEAT_NS`/`seatFromThisTab`/`seatHeldByOtherTab`.
- 서버는 무죄였다(`node scripts/online-test.js`의 재접속 시나리오·ws 레벨 순서 바꿔가며 확인해도 정상). 클라 저장소 문제였다.

## 이전 세션에 한 것 (v1.142 → v1.153)

윷·요트 "넷마블/한게임급 고도화" 방향으로 진행. 핵심은 **실제 3D 캐릭터 아트 도입**.

| 버전 | 내용 |
|------|------|
| v1.142~143 | **선 뽑기 이해도** — 라운드 결과를 페이싱(엔진 setTimeout, 일반 2.0s/동점 2.8s)으로 화면에 남겨 보이게, "동점 재대결" 명시, 확정 순서/재대결 분리 |
| v1.144 | 윷 **무대 배경** — 검은 void → 따뜻한 조명(등불·후광·상 반사·벽 비네트, 전부 정적) |
| v1.145 | 던지기 결과 오버레이 톤다운(도·개·걸 작게) + 선 뽑기 우측 열 중복 정리 |
| v1.146 | **첫 판 인터랙티브 튜토리얼**(코치마크 3스텝, 1회성 `yut_tut`) |
| v1.147 | 결과 화면 **우승자 히어로**(실제 우승 캐릭터를 유광·왕관·컨페티로) |
| v1.148 | **레벨·타이틀 진행**(XP=15+승25+잡기, 10단계, 셋업 레벨바 + 결과 성장박스 레벨업) — 아트 불필요 메타 |
| v1.149 | **무대 배경 4종 확산**(kb·ld·lcr·yacht, 조크 테마 excel·photoshop 제외) |
| — | **`ART_BRIEF.md`** 추가(발주 브리프 + AI 프롬프트) |
| v1.150 | **캐릭터 3D 아트 교체 5종** — 평면 PNG → 소프트 3D 마스코트(돼지·개·양·소·말). 파일명 동일(`img/{동물}.png`)이라 코드 변경 0 |
| v1.151 | **칭호 장착**(레벨 해금 타이틀 선택 → 인게임 카드 표시, `yut_title`) |
| v1.152 | 윷 **빽도 첫 진입 규칙** — 판에 말 없을 때 빽도 → 낙 대신 **출발점(node 0)에 서기**, 이후 도~모로 전진 |
| v1.153 | 요트 UX 3건 — 반응/말풍선 위치(점수판 꼭대기 → 큰 턴카드), 내 차례 박스 축소(244→178px), 채팅 씹힘(throttle·드롭 제거 → 즉시 표시·전송) |

또: `cdp.js`에 **`--mute-audio`** 추가(검증 중 소리 안 나게).

## 🔴 다음 최우선 — 캐릭터 표정 시트 (사용자가 생성 중)

**v1.150에서 캐릭터를 3D로 교체했고, 코드에 표정 리액션 훅이 이미 있다**(잡음/완주/늪 등). 사용자가 **표정 변형 6종 × 5동물 = 30장**을 같은 스타일로 생성해서 줄 예정. 오면:

1. **처리**: `python3 scripts/process-char-art.py <생성본> public/img/pig_happy.png` (배경 자동 제거 + 리사이즈 + 양자화). ← 이번 세션에 만든 재사용 스크립트.
2. **연결**: 리액션 발생 지점에서 `ANIMAL_IMG[type]` 대신 표정 이미지로 스왑. 표정↔상황 매핑:
   - `happy` 잡음·완주 / `sad` 잡힘·패배 / `surprise` 늪·빽도 / `angry` 당함 / `star` 윷·모·승리 / `cheer` 최종 우승 히어로
   - 파일명 규칙: `pig_idle.png`(=현재 pig.png), `pig_happy.png`, `pig_sad.png` …
3. 리액션 훅 위치: yut.html의 `reactPiece`/`_emote`(판 위 말), `renderResHero`(우승), 캐릭터 카드(`renderCharCards`), floatReaction 등. **표정 이미지 스왑 + 원위치 복귀** 방식이 깔끔.

> 스타일/프레이밍/배경 프롬프트는 `ART_BRIEF.md`에 다 있음.

## 그 밖의 대기/다음 후보

1. **말(horse) bust 재생성** — 5종 중 말만 원본이 전신이라 자동 크롭(상단 60%)으로 얼굴이 조금 작다. 사용자가 상반신으로 재생성하면 `process-char-art.py`로 교체(이번엔 `--bust` 없이). 지금도 무난.
2. **캐릭터 스킨/상점** — 스킨 아트(한복·왕관 등 코스튬)가 오면 상점 UI + 장착 + 레벨 해금 붙이기. 레벨/칭호 시스템(v1.148·1.151)에 연결. 아트 없이는 칭호 장착까지가 한계(이미 함).
3. ~~채팅 2인 실측~~ — **완료(v1.154~155)**. 자리 뺏기(새로고침·새 탭) 둘 다 막았고 `npm run test:online2p`가 지킨다. 반증도 확인함(`delete window.BroadcastChannel`로 옛 동작 재현 → 그대로 자리 뺏김).
4. ~~남은 UX 후보 3건~~ — **완료(v1.157)**. 남은 관찰: 모바일 코치마크는 화면이 빽빽해 어디에 놓아도 무언가는 가린다(현재는 미션 패널만 가림 — 최선). 더 줄이려면 스텝별로 대상을 좁히는 편이 낫다.
5. **kb·ld·lcr 캐릭터 업그레이드** — 이들은 자체 아바타(이모지 등) 사용. 윷·요트가 화려해진 만큼 상대적으로 밋밋. 3D 캐릭터/폴리시 확산 여지.
6. **인게임 메타 연동 5종** — 레벨/XP 연출을 윷 외 게임에도.

## 🎨 아트 후처리 파이프라인 (이번 세션 신설)

`scripts/process-char-art.py` — 생성본을 게임 PNG로. **핵심 교훈:**
- ChatGPT/DALL·E "투명"은 **거짓**(RGB에 밝은 체커보드가 구워짐). 코너 flood-fill로 제거.
- 배경 체커 = 무채색(`max-min<8`), 캐릭터 밝은 부위(양털·소흰색) = 따뜻한 색조(`max-min≥15`) → `isbg = min>225 and max-min<8`로 정확히 구분.
- 트림→정사각(여백 14%)→400px→FASTOCTREE 256색 양자화(엣지 alpha 보존, ~25KB, 원본 평면 수준 무게).
- 전신 소스는 `--bust 0.60`으로 상반신 크롭.

## 🔑 브라우저 자동 검증 (`scripts/browser-test/`)

Playwright 크로미움 캐시를 `ws`로 CDP 직접 제어(새 의존성 0). **연출·레이아웃은 반드시 눈으로.**
```bash
node server.js                                   # 먼저 서버(또는 ensureServer가 알아서)
node scripts/browser-test/verify-fx.js           # 자동 단언
node scripts/browser-test/capture.js hold        # 연출 눈으로 → out/*.png
```
- **`cdp.js`에 `--mute-audio`·`page.setMotion(true)` 있음.** 헤드리스 기본 `prefers-reduced-motion:reduce`라 안 켜면 연출 전부 생략된 화면을 본다(오진 주의, CLAUDE.md #7).
- 게임 상태 `S`는 IIFE 스코프 → DOM으로 판정. 윷 던지기는 `mousedown`→대기→`window` `mouseup`.
- **closure 함수 검증 팁**: 임시 시임 `try{window.__x=fn}catch(_){}` `/* TEMP-... */` 넣고 테스트, **커밋 전 `perl -0pi -e` 로 제거**(이번 세션에 여러 번 씀).
- yut 외 게임 조작은 헬퍼가 없어 CDP 직접(`page.eval`로 버튼 클릭). yacht: `#tabOnline`→`#createRoom`→`#addAiBtn`→`#startOnline`.

## 반드시 지킬 규칙

- **엔진 이중구조**: 엔진 로직은 `public/*-core.js`만 고치고 `npm run build`. HTML `<!-- CORE:x -->` 마커 안은 직접 수정 금지. UI·CSS·연출은 마커 바깥 HTML 직접 수정.
- 엔진 바꿨으면 `buildPath↔step`, `simStep↔step` 일치 확인(v1.152 빽도가 이 3곳 동시 수정 사례).
- **모션은 reduced-motion에서 생략.** `FX_REDUCED()`는 yut에만 있음 — 다른 게임은 `matchMedia` 직접.
- **정적 연출(무대·볼륨·그림자)은 reduced에서도 유지**(모션 아님).
- **과하지 않게.** v1.127에서 "정신없다" 피드백으로 연출을 걷어낸 이력 존중 — 표현은 판에 흩뿌리지 말고 큰 카드/캐릭터에 집중.
- 검증: `npm run check:drift` → `<script>` 파싱 → 연출이면 캡처로 눈으로.
- 응답·주석 한국어, 정확성 우선(재성은 틀리면 바로 지적). 작업마다 `package.json` 버전↑, 커밋 끝에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **푸시=배포. 사용자 확인받고.** (이번 세션은 각 기능마다 확인 후 푸시)

## 프로젝트 빠른 참조

- 로컬: `node server.js` → localhost:3000 · 배포: `git push` → Render(`yacht-dice-jxva.onrender.com`) → Electron이 그 URL 로드
- 게임: yut/yacht/kb/ld/lcr `.html` + 각 `*-core.js`(엔진) + `index.html`(허브)
- 문서: `CLAUDE.md`(아키텍처·함정), `ART_BRIEF.md`(아트 발주), `scripts/browser-test/README.md`(검증)
- 상태 키(localStorage, yut): `yut_stats`(games/wins/xp…), `yut_title`(칭호), `yut_tut`(튜토리얼 봄), `yut_char`, `alley_theme`
