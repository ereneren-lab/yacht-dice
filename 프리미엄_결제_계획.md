# 프리미엄 실결제 — 착수 계획 (2026-07-28 예정)

`다음단계.md` #4의 상세 계획. **내일 이 문서부터 읽고 시작.**
현재 코드를 직접 뒤져 확인한 사실만 적었다(추측 아님).

---

## ✅ 결정 완료 (2026-07-28 · 재성님)

1절의 결정 A~D를 전부 받았다. **아래가 확정이며, 코드 착수 때 이대로 간다.**

| 결정 | 답 | 의미 |
|---|---|---|
| **A. 어디서 파나** | **웹/PWA 먼저** | 스토어 IAP(Play Billing·StoreKit)는 나중. 심사·계약·세금 설정이 붙어 하루에 안 끝난다 |
| **B. 구매 귀속** | **복구 코드** | 계정 없음 유지. 결제 → 서버가 `ALLEY-XXXX-XXXX` 발급 → 앱에 입력 → 서버 검증 |
| **C. 무엇을 파나** | **둘 다 — 광고 제거 + 부가 혜택** | 광고를 다시 켜고, 프리미엄에 테마·스킨 등도 얹어 값어치를 올린다 |
| **D. 가격** | 미정 | 웹 단건. C가 '둘 다'라 값어치가 올라간 만큼 가격도 그 뒤에 정한다 |

### ⚠️ C가 '둘 다'라서 생긴 선행 작업 (잊지 말 것)

- **광고를 다시 켜야 한다** — `monetize.js`의 `ADS_ENABLED=false`를 되돌리고,
  **MainActivity의 `AD_INSET_DP`(웹뷰 하단 인셋)도 함께** 되돌려야 한다(주석에 그렇게 적혀 있다).
- 광고를 켜면 **v1.205에 껐던 이유가 그대로 돌아온다** — 배너가 폰에서 세로 52px를 뺏는다.
  v1.228~v1.239에서 13종 × 8뷰포트 넘침을 0으로 만들어놨는데, **배너를 켜면 그 검사를 다시 돌려야 한다.**
  → 광고를 켜는 커밋에서 반드시 `npm run audit:ux` 전체 재실행.
- **부가 혜택(테마·스킨)은 아직 만들어진 게 없다.** '팔 물건을 만드는' 작업이 별도로 필요하다.

### 착수 순서 — 유입 실험이 먼저다 (2026-07-28 결정)

재성님 결정: **"결제는 법적 준비만 병렬 + 유입 먼저."**
결제 코드는 방문자 숫자를 한 번 본 뒤에 붙인다. 이유는 3절의 3번 그대로다 —
사업자등록·통신판매업 신고까지 하는 비용을 쓰기 전에 실제 방문자를 보는 편이 낫다.

---

## 0. 먼저 알아야 할 것 — 지금 상태 (실측)

| 항목 | 현재 | 파일 |
|---|---|---|
| `MZ.buyPremium()` | **무조건 `_setPremium(true)`** — 콘솔에 한 줄이면 공짜 프리미엄 | `public/monetize.js:119` |
| 프리미엄 저장 | `localStorage['alley_premium'] = '1'` **그게 전부** | `monetize.js:25` |
| 광고 | **꺼져 있음** (`ADS_ENABLED = false`) | `monetize.js:32` |
| 계정·로그인 | **없음** | 저장소 전체 |
| 서버 영속 저장소 | **없음** (방은 메모리 `Map`, 재시작하면 사라짐) | `server.js:88` |
| 상점의 프리미엄 버튼 | `준비 중`으로 비활성 | `shop.html:156` |
| 약관·환불 페이지 | **없음** (`privacy.html`만 있음) | `public/` |

### ⚠️ 착수 전에 짚고 갈 것 (한 문장씩)

1. **지금 프리미엄은 팔 물건이 없다.** 프리미엄 = '광고 제거'인데 광고가 꺼져 있다.
   **광고를 먼저 켜지 않으면 돈 내고 사도 달라지는 게 없다.** 순서가 있다(아래 1단계).
2. **구매를 묶을 대상이 없다.** 계정도 서버 저장소도 없어서 결제해도 `localStorage` 플래그만 남는다
   → 기기 바꾸면 사라지고, 브라우저 데이터 지우면 사라지고, 개발자도구로 위조된다.
   **이게 가장 큰 구조적 공백이고, 결제 코드보다 먼저 정할 문제다.**
3. **아직 방문자 수를 모른다.** 계측을 넣은 게 며칠 전이다(`유입실험_계획.md`).
   파는 것 자체는 말리지 않지만, **사업자등록·통신판매업 신고까지 하는 비용**을 쓰기 전에
   Plausible에서 실제 방문자를 한 번 보고 결정하는 편이 낫다.

---

## 1. 결정할 것 4가지 (코드보다 먼저)

### A. 어디서 파나 — **경로가 둘로 갈린다. 하나로 못 합친다.**

| 판매처 | 결제 수단 | 비고 |
|---|---|---|
| 웹 / PWA | Stripe · 토스페이먼츠 · 아임포트 등 자유 | 수수료 3%대 |
| Google Play | **Play Billing 필수** | 디지털 재화에 외부 결제 금지(예외 좁음) · 수수료 15~30% |
| App Store | **StoreKit 필수** | 〃 |

이 저장소엔 `android/`·`ios/`(Capacitor)가 다 있다. **스토어에 올릴 거면 그쪽 결제를 따로 붙여야 한다.**
→ 권장: **웹부터.** 스토어 IAP는 심사·계약·세금 설정이 붙어서 하루에 끝나지 않는다.

### B. 구매를 무엇에 묶나 — **핵심 결정**

| 방식 | 장점 | 비용 |
|---|---|---|
| **① 복구 코드** (권장) | 계정 불필요 · 인프라 최소 · 기기 이전 가능 | 코드를 잃으면 사용자가 곤란 |
| ② 계정(이메일 로그인) | 정석 · 기기 이전 자유 | 로그인 UI + DB + 비번/메일 발송 — 큰 작업 |
| ③ 지금처럼 localStorage만 | 즉시 | **결제받고 기기 바꾸면 사라진다 → 환불 분쟁. 실결제엔 부적합** |

**① 복구 코드 흐름**: 결제 완료 → 서버가 코드 발급(예: `ALLEY-XXXX-XXXX`) → 화면 표시 + 메일 발송
→ 사용자가 앱에서 코드 입력 → 서버 검증 → 프리미엄 켜짐. 코드는 서버 DB에 저장.

### C. 광고를 켤 것인가
프리미엄이 상품이 되려면 광고가 있어야 한다. 다만 v1.205에 **의도적으로 껐다**
("게임 우선 · 광고가 세로 공간을 뺏는다"). 다시 켜면 그 문제가 돌아온다.
→ 대안: 프리미엄을 '광고 제거'가 아니라 **다른 것**으로 팔 수도 있다(테마·주사위 스킨·통계 등).
   그러면 광고를 안 켜고도 팔 물건이 생긴다. **이건 상품 결정이라 재성님 몫.**

### D. 가격
웹 기준 단건 결제. 스토어를 병행하면 수수료 차이 때문에 가격을 맞출지 갈릴지도 정해야 한다.

---

## 2. 법적 준비 (한국 · 유료 판매 시 필수)

코드보다 이게 리드타임이 길다. **먼저 시작해 두는 게 낫다.**

- [ ] **사업자등록** — 유료 판매엔 필요 ← **재성님만 할 수 있음 · 리드타임 최장**
- [ ] **통신판매업 신고** — 온라인 유료 판매 시 (사업자등록이 선행)
- [ ] **판매자 정보 고지** — 상호·대표자·사업자번호·주소·연락처 (전자상거래법)
      → **페이지는 만들어 뒀다**(`terms.html` 하단 표). 위 두 항목이 끝나면 **값만 채우면 된다.**
- [x] **환불(청약철회) 정책 페이지** — ✅ `public/refund.html` (2026-07-28 초안)
- [x] **이용약관 페이지** — ✅ `public/terms.html` (2026-07-28 초안)
- [ ] 결제대행사 심사 (Stripe는 국내 사업자 조건 확인 · 토스페이먼츠는 계약 필요)

### 만들어 둔 두 페이지에 대해 (2026-07-28)

`privacy.html`과 같은 톤·스타일로 맞췄고 `noindex`다. **로컬 200 확인.**

- ⚠️ **아직 어디서도 링크하지 않았다.** 허브 푸터엔 `privacy.html`만 걸려 있다(`index.html:527`).
  약관·환불 링크를 허브에 걸면 **파는 게 있다는 신호**가 되므로, 결제를 여는 커밋에서 함께 건다.
- ⚠️ **두 페이지 다 맨 위에 '초안' 배너가 있다.** 사업자 정보를 못 채운 채 판매 페이지처럼
  보이는 게 더 위험해서 넣었다. **등록이 끝나면 배너를 지우고 표의 `— 미정 —`을 채울 것.**
- 환불 정책의 뼈대(코드 미등록 7일 전액 / 등록 후 제한 / **하자·표시광고 불일치는 사용 후에도 환불**)는
  전자상거래법 제17조를 따라 썼다. **다만 나는 변호사가 아니다** — 실제 판매 전에 한 번 검토받을 것.
- 제17조의 청약철회 제한은 **구매 화면에서 미리 고지하고 동의를 받아야** 적용된다.
  → 3-4의 결제 UI에 **'디지털 콘텐츠 청약철회 제한 동의' 체크박스가 반드시 있어야 한다.**

---

## 3. 기술 작업 순서 (결정 A~D가 끝난 뒤)

### 3-1. 서버에 영속 저장소 붙이기 ← **선행 필수**
지금 서버는 저장소가 **아예 없다**(메모리 Map만). Render 무료 인스턴스는 디스크도 휘발성이라
파일로도 못 남긴다. → Render Postgres / Supabase / Turso 중 하나.
- 테이블 최소 스펙: `purchases(id, code, email, provider, provider_ref, amount, created_at, redeemed_at, device_hint)`

### 3-2. 결제 연결
- Stripe라면 **Payment Link + Webhook**이 가장 적은 코드다(체크아웃 UI를 안 만들어도 된다).
- ⚠️ **웹훅 검증 필수** — 서명 확인 없이 신뢰하면 누구나 가짜 결제완료를 쏴서 코드를 받아간다.
- ⚠️ **멱등 처리** — 웹훅은 재전송된다. `provider_ref` 유니크 제약으로 중복 발급을 막을 것.

### 3-3. `monetize.js` 고치기
```
buyPremium()   → 결제 페이지로 보내기 (지금처럼 바로 true 주지 말 것)
redeemCode(c)  → 서버에 코드 검증 요청 → 성공 시에만 _setPremium(true)
restorePremium() → 저장된 코드로 재검증
```
⚠️ **`_setPremium(true)`를 클라이언트 판단만으로 부르는 경로가 하나도 남으면 안 된다.**
지금 `buyPremium`이 정확히 그 상태다.

### 3-4. UI
- `shop.html`의 '준비 중' 버튼 활성화 + 가격 표시
- 코드 입력 칸 + '구매 복원' 버튼
- 약관·환불 링크 (법적 고지 의무)

### 3-5. 검증 (이 저장소 관례대로 테스트 추가)
- [ ] Stripe **테스트 모드**로 결제 → 코드 발급 → 적용까지 전 구간
- [ ] 위조 방어: 콘솔에서 `MZ._setPremium(true)` 해도 **서버 검증 없인 무효**여야 한다
- [ ] 웹훅 서명 위조 거부 · 웹훅 재전송 시 코드 1개만 발급(멱등)
- [ ] 같은 코드 두 기기 사용 정책 결정(허용? 제한?) 후 그대로 테스트
- [ ] 환불 처리 흐름(코드 무효화)
- [ ] `npm run test:online` 등 기존 회귀 — 서버에 DB가 붙으므로 부팅 경로 확인

---

## 4. 내일 이 순서로 하면 됨

1. **결정 A~D를 먼저 답한다** (30분) — 여기서 안 정하면 코드가 헛돈다
2. 법적 준비 항목을 **병렬로 착수**(리드타임이 길다)
3. C에서 '광고를 안 켜고 다른 걸 판다'로 정해지면 → **팔 물건부터 만든다**
4. 그다음 3-1 → 3-2 → 3-3 → 3-4 → 3-5

**하루에 다 안 끝난다.** 3-1(DB)까지만 해도 하루 분량이다.
가장 값싼 첫걸음은 **1번 결정 + 2번 착수**다.

---

## 참고 — 손댈 파일
- `public/monetize.js` — `buyPremium`/`restorePremium`/`_setPremium` (119~137행)
- `public/shop.html` — 프리미엄 카드 (154~160행)
- `public/index.html` — `body.premium` CSS (143~149행) · 배선 (770행~)
- `server.js` — 결제 웹훅 라우트 + DB (지금 정적 서빙 + WS만 있다)
- 새로: 약관·환불 페이지, 코드 입력 UI
