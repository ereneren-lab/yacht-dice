# 다음 단계 — 2026-07-28 세션 인수인계 (v1.240)

이 파일은 세션이 끊겨도 이어서 작업할 수 있게 남기는 메모다. **항상 여기부터 읽을 것.**
**현재: v1.240** — 푸시·배포 완료. 13게임 아이템전 · **온라인 10종(사람끼리 붙는 게임 전부)** · 셋업 넘침 0.

---

## 🔴 2026-07-28 세션 — 지금 막힌 것부터 (게임 코드는 안 건드렸다)

이 날은 **게임을 만들지 않았다.** 결제·유입 쪽 결정과 준비만 했다.

### 재성님 결정 4건 (확정)
| 결정 | 답 |
|---|---|
| 오늘 순서 | **결제는 법적 준비만 병렬 + 유입 먼저** |
| 프리미엄 상품 (C) | **둘 다 — 광고 제거 + 부가 혜택(테마·스킨)** |
| 구매 귀속 (B) | **복구 코드** (계정 없음 유지) |
| 판매처 (A) | **웹/PWA 먼저** (스토어 IAP는 나중) |

→ 상세는 `프리미엄_결제_계획.md` 맨 위 '결정 완료' 절.

### 한 일
- `public/terms.html` · `public/refund.html` **초안 작성** (privacy.html과 같은 톤 · `noindex` · 로컬 200)
  - **아직 어디서도 링크 안 했다.** 허브 푸터엔 `privacy.html`만 있다(`index.html:527`).
    약관·환불 링크를 걸면 '파는 게 있다'는 신호라, 결제를 여는 커밋에서 함께 건다.
  - 두 페이지 다 맨 위 **'초안' 배너** + 판매자 정보 `— 미정 —`. 사업자등록이 끝나면
    **배너를 지우고 값만 채우면 된다.**
- **콜드스타트 실측** — 아래가 이 세션의 핵심 발견이다.

### 🔴 다음 세션이 가장 먼저 볼 것 — 콜드스타트 12.95초
| 상태 | TTFB |
|---|---|
| **콜드(잠든 뒤 첫 요청)** | **12.95초** |
| 웜 | 0.73~1.08초 |
| 요트 직행(실험 A 착지)·웜 | 0.54초 |

**유입 실험을 이대로 쏘면 안 된다.** 커뮤니티 유입은 대부분 콜드로 떨어져서
13초 흰 화면을 본다 → 숫자가 낮게 나와도 **게임 탓인지 서버 탓인지 구분이 안 된다.**
막는 법과 각 안의 장단은 `유입실험_계획.md` **5-1절**에 정리했다
(권장: 외부 핑 5분 간격 · Render 무료 750시간 한도 주의 · GitHub Actions는 지연 때문에 비권장).

### ⬜ 다음에 할 일 (순서대로)
1. **콜드스타트 막기** — 재성님이 UptimeRobot 등에 모니터 1개 등록(5분). 내가 대신 못 한다.
2. Plausible 대시보드에 사이트가 실제로 등록돼 있는지 확인 + 방문 1회 찍히는지 눈으로
3. 커뮤니티 홍보 규정 확인 → **실험 A 발사**
4. (병렬) 사업자등록 · 통신판매업 신고 — 리드타임 최장
5. 숫자를 본 뒤에 결제 코드 (`프리미엄_결제_계획.md` 3절)

| 버전 | 내용 |
|---|---|
| v1.227 | 가로 회전 대응 · 튜토리얼 13종 · 아이템전 6종 · 허브 카테고리 필터 |
| v1.228 | 인게임 스크롤 0 — 13게임 × 8뷰포트 전부 |
| v1.229 | 셋업 길이 축소 — 세로 4폭에서 13게임 전부 0 |
| v1.230 | 가로 셋업 옵션 2열(라벨:필드 배치) |
| v1.231 | 알까기 아이템전 — 아이템전 7종 |
| v1.232 | 인디언 포커 코어 분리 + 아이템전 — 코어 8종 · 아이템전 8종 |
| v1.233 | 원카드 코어 분리 + 아이템전 — 코어 9종 · 아이템전 9종 |
| v1.234 | 도둑잡기 코어 분리 + 아이템전 — 코어 10종 · 아이템전 10종 · A갈래 완료 |
| v1.235 | B갈래 3종 아이템전(블랙잭·바카라·하이로우) — 아이템전 13종 = 전 게임 |
| v1.236 | 카드 3종 온라인 — 서버 라우팅 (인디언·원카드·도둑잡기) |
| v1.237 | 카드 3종 온라인 완성 — 공용 모듈 `net.js` · 온라인 9종 |
| v1.238 | 도둑잡기 자리 편향 해소 · 인디언 판 길이 단축 · 상점 죽은 코드 제거 |
| v1.239 | 셋업 넘침 완전 해소 — 요트·알까기·섯다 접기(13종 × 8뷰포트 전부 0) |
| **v1.240** | **섯다 온라인화** — 온라인 10종 · 사람끼리 붙는 게임은 이제 전부 온라인 |

---

## v1.240에서 한 일 (섯다 온라인화 — 온라인 10종)

**재성님이 "안 된 게 왜 이렇게 많냐"고 물어서 세어보니 섯다 하나가 진짜 빠져 있었다.**
코어(`seotda-core.js`)는 이미 있는데 서버 화이트리스트에만 안 들어가 있었다 —
지난 작업에서 '카드 3종'만 보고 섯다를 놓쳤다.

### 이제 온라인이 아닌 3종은 **일부러** 그런 것이다
블랙잭·바카라·하이로우는 **상대가 딜러(하우스)**다. 방을 만들어도 각자 딜러랑 따로 노는 걸
구경하는 꼴이라 온라인이 의미가 없다(코어도 일부러 안 뺐다). **사람끼리 붙는 10종은 전부 온라인이다.**

### 섯다에서 따로 손봐야 했던 것
- **AI 구동 주체가 다르다** — 로컬은 `manualAI:true`로 두고 클라의 `drive()`가 연출을 다 보여준 뒤
  `aiTurnIfNeeded()`를 민다. **온라인은 서버가 주인**이라 `manualAI`를 끄고 엔진이 스스로 돌게 했고,
  클라의 `drive()`는 온라인이면 진행을 안 밀고 연출만 한다(안 그러면 양쪽이 같이 밀어 꼬인다).
- **지갑** — `walletBuyIn`은 로컬 전용이다. 온라인은 `startGame()`을 안 거치니 null로 남아
  `cashOut()`이 no-op이 된다 → 온라인 칩이 보유 머니로 흘러들지 않는다(확인함).

### 🐛 온라인에서만 터진 버그 둘 (2탭 e2e가 잡았다)
1. **`mySeat()`가 `MYPID`('me')를 찾는다** — 온라인 pid는 서버가 주는 값이라 `findIndex`가 -1을 내고
   `render()`가 `undefined.cardCount`에서 죽었다. 화면 앞부분은 이미 그려진 뒤라
   '게임은 되는데 일부만 안 됨'으로 보인다(함정 #2). → `myPid()`로 갈랐다. 승패 판정 2곳도 같이 고쳤다.
2. **아바타를 `<img src>`로 감쌌다** — 서버 아바타는 이모지('🦊')라 404가 났다.
   로컬은 아바타가 없어서(null) 안 드러나던 버그다. → 경로처럼 보일 때만 `<img>`, 아니면 글자.

**둘 다 '완주는 했는데 콘솔 에러'로 잡혔다** — 단언에 콘솔 에러 검사를 넣어둔 값을 했다.

### 검증
```
npm run test:cardonline   2탭 e2e 4종(원카드·도둑잡기·인디언·섯다) 전부 통과
npm run test:online       서버 e2e 10종 + 누출 단언
audit:ux seotda           8뷰포트 0 — 온라인 버튼을 넣자 다시 넘쳐서(+55px) 시작 버튼과
                          한 줄로 합치고 max-height 압축을 더했다(함정 #8: 높이는 max-height로)
test:items:more · test:stats · test:tutorial · check:drift 전부 통과
```

---

## v1.239에서 한 일 (셋업 넘침 해소 — #5 완료)

요트·알까기·섯다 셋업을 `<details>`로 접었다. **13게임 × 8뷰포트 셋업 넘침이 전부 0이 됐다.**

### 문서가 틀렸던 부분
"세로는 13종 전부 0"이라고 적혀 있었지만 **실측은 아니었다** —
소형폰 세로(360×640)에서 알까기 +392px · 요트 +89px · 섯다 +83px, 태블릿에서도 새고 있었다.

| 게임 | 고치기 전 (최악) | 고친 뒤 |
|---|---|---|
| 알까기 | 640×360 +450px · 360×640 +392px | 8뷰포트 전부 0 |
| 요트 | 640×360 +364px · 844×390 +224px | 8뷰포트 전부 0 |
| 섯다 | 640×360 +315px · 844×390 +177px | 8뷰포트 전부 0 |

### ⚠️ 핵심 — 높이 임계값으로 접으면 반드시 틀린다
처음엔 `innerHeight >= 680`이면 펴는 식으로 짰다. **알까기가 844px 세로에서도 넘쳤다**(+254px).
게임마다 옵션 길이가 다르니 고정 숫자는 어느 게임에선 늘 틀린다.
→ **일단 펼쳐보고 `scrollHeight > innerHeight`면 그때 접는다.** 게임별 튜닝이 필요 없고
옵션이 늘어도 알아서 맞는다. CLAUDE.md 함정 #20에 적어뒀다.

- 요트는 접을 게 둘이라(기존 `#setupExtra` 기록·테마 / 새 `.setup-opts` 판 설정)
  **덜 중요한 기록·테마부터** 접고, 그래도 넘치면 판 설정을 접는다.
- 사용자가 직접 여닫으면 `dataset.touched`로 표시하고 더는 자동으로 안 건드린다.
  자동 조작 중엔 `dataset.auto`를 세워 자기 이벤트를 '사용자 조작'으로 오인하지 않게 했다.

---

## v1.238에서 한 일 (밸런스 2건 + 정리)

### 도둑잡기 자리 편향 — 해소 (재성님 결정: 시작 자리 무작위)
**시작 차례와 분배 시작 자리를 둘 다 매 판 무작위로** 돌렸다. 규칙은 하나도 안 바꿨다 —
누구부터 돌리고 누가 먼저 하느냐만 돌린 것이라 실물 게임과 같다.

| 방식 | 3인 편차 | 4인 편차 | 5인 편차 |
|---|---|---|---|
| 고정시작 · 고정분배 (원래) | 20.9pt (1.96배) | 2.3pt | 18.4pt (**3.46배**) |
| 무작위시작만 | 6.4pt | **8.0pt (오히려 악화)** | 9.2pt |
| **둘 다 무작위 (채택)** | **1.7pt** | **1.2pt** | **0.8pt** (최대 1.05배) |

⚠️ **무작위 시작만으로는 부족했다** — 4인이 오히려 나빠졌다. 남은 원인이 분배였기 때문:
53장이라 한 자리가 1장 더 받는데(4인 14/13/13/13) **늘 0번이 그 한 장을 받고 있었다.**
- **카드를 빼서 균등하게 맞추는 건 불가능하다.** 총합이 홀수(조커 1 + 짝 N개)여야 해서
  짝수 인원에선 균등 분배가 성립하지 않고, 억지로 빼면 짝이 안 맞는 카드가 남아
  **'술래 없는 판'** 이 나온다(5인에서 79%가 그렇게 끝났다 — 실험으로 확인).

### 인디언 포커 판 길이 — 단축 (재성님 결정: 시작 칩 낮추기)
시뮬로 **실측하고** 숫자를 골랐다(감으로 정하지 않았다). 중앙값 판수:

| 시작칩 | 2인 | 3인 | 4인 |
|---|---|---|---|
| 5 | 10 | 23 | 38 |
| **10 (새 기본)** | **35** | **82** | **144** |
| 20 | 131 | 328 | 582 |
| ~~50 (옛 기본)~~ | ~~790~~ | ~~2,073~~ | ~~3,657~~ |

옵션을 `5 · 짧게 / 10 · 보통 / 20 · 길게`로 바꾸고 기본을 10으로 내렸다. 규칙은 그대로다.

### 🐛 같이 고친 버그 — 온라인 인디언 칩 옵션이 안 먹었다 (v1.236 회귀)
방 생성 검증이 **좌·중·우 범위(`[3,4,5]`)만** 보고 있어서, 인디언이 무엇을 보내든
기본값으로 떨어졌다 → **온라인 인디언은 늘 50칩**이었다. 게임별로 갈라서 검증한다.
회귀 방지: `startChips`는 게임마다 의미도 범위도 다르다는 걸 잊지 말 것.

### 상점 죽은 코드 제거
소모품 판매를 내린 뒤 남아 있던 `SHOP.count`/`SHOP.spend`, `buy()`의 `use` 분기,
`shop.html`의 `isUse` 분기를 지웠다(`slot:'use'` 물건이 하나도 없다).
⚠️ **`refundConsumables()`와 `d.use` 정규화는 남겼다** — 예전에 산 사람의 잔여 개수를
읽어 코인으로 돌려주는 마이그레이션이라, 지우면 조용히 물건만 사라진다.

---

## v1.237에서 한 일 (카드 온라인 완성 — 공용 모듈)

**재성님 결정: A안(공용 모듈).** `public/net.js`(372줄)를 만들고 카드 3종이 이걸 쓴다.

- **모듈이 갖는 것**: 소켓·재접속·콜드스타트 배너 · 자리 소유권(함정 #8) ·
  **판 종료 → 로비 전환(함정 #3)** · 로비 UI 자체 주입(tutorial.js 방식).
  `_inGame`을 스스로 추적해 `onGameEnd`를 `onLobby`보다 **먼저** 부른다 — 이 순서가 함정 #3의 핵심이다.
- **게임 쪽 추가 코드는 게임당 약 35줄**이다(기존 방식이었으면 140줄 × 3).
  `NET.init` + `NET.ui.mount` + `act(a)` 한 곳으로 액션 분기.
- **로컬과 온라인이 같은 `render()`/`applyFx()`를 쓴다** — 코어를 미리 분리해둔 덕에
  스냅샷 모양이 같아서 온라인용 렌더를 따로 만들 필요가 없었다. 이게 A·B갈래 작업의 배당금이다.
- 인디언 코어에 `{type:'nextHand'}` 액션을 추가했다 — 온라인에선 '다음 판'을 누를 사람이
  정해져 있지 않아 앉은 누구나 넘길 수 있게(단 `phase==='result'`일 때만).

### 검증 (전부 통과)
```
npm run test:cardonline   2탭 e2e 3종 — 방 생성·참가·대기실·시작·숨김정보·판 종료 후 복귀(함정 #3)
npm run test:online       서버 e2e 9종 + 누출 단언 (브라우저 불필요)
npm run test:online2p     기존 6종 자리 소유권 회귀 — 이상 없음
test:items:more 10종 · test:stats 7종 · test:tutorial 13종 · check:drift 10종
```

### ⬜ 남은 것 — 기존 6종의 net.js 이관(선택)
지금은 **카드 3종만** net.js를 쓰고 기존 6종은 인라인 로비 코드를 그대로 갖고 있다.
당장 문제는 없지만(둘 다 같은 서버 프로토콜) 로비 버그를 고칠 때 두 군데를 봐야 한다.
급하지 않다 — 기존 6종은 오래 돌아가며 다듬어진 코드라 **건드리는 위험이 이득보다 클 수 있다.**
새 게임을 온라인화할 땐 net.js를 쓸 것.

---

## v1.236에서 한 일 (카드 온라인화 — 서버 절반)

`server.js`에 인디언 포커·원카드·도둑잡기 라우팅을 붙였다. **서버는 끝났고 클라 로비가 남았다.**

- 코어 3종 `require` + `ONLINE_GAMES` 화이트리스트 + `CAP`(인디언 6 · 원카드 4 · 도둑잡기 5)
  + `MIN_PLAYERS`(도둑잡기 3 — 2인이면 서로 뽑기만 반복돼 판이 안 선다).
- 방 생성의 긴 삼항 사슬을 `ONLINE_GAMES` Set으로 정리했다(동작은 그대로).
- **셋 다 `ld` 방식으로 붙였다** — 숨김정보가 있어서 `broadcast`가 아니라
  `room.members.forEach(mm => send(mm.ws, engine.serialize(mm.pid)))`로 **사람마다 다른 스냅샷**을 보낸다.
  이걸 broadcast로 하면 그 순간 게임이 통째로 무너진다.

### 검증 — `npm run test:online`에 3종 + 누출 검사 추가 (브라우저 불필요)
같은 순간 두 클라의 스냅샷을 비교해 **남의 것이 실려 오는지**를 직접 본다:
- 인디언: `myCard`가 양쪽 다 null이고 남의 `card`는 보임(이 게임만 방향이 반대)
- 원카드: `hands`/`draw` 배열 미포함 · 두 사람의 `myHand`가 서로 다름
- 도둑잡기: 조커(`"joker":true`) 미노출 · 남의 `hand` 미포함 · 손패 서로 다름
기존 6종 + 재접속 시나리오도 전부 통과(회귀 없음).

### ⬜ 남은 일 — 클라 로비 3종
서버는 받을 준비가 끝났고, **각 HTML에 로비 UI와 소켓 코드가 없다.** 기존 게임 기준
게임당 온라인 관련 코드가 **약 140줄**(lcr 1,661줄 중 139줄)이다. 붙일 것:
1. 방 만들기/참가 UI + 방 코드 공유 + 대기실(멤버 목록·AI 추가·시작)
2. `wsSend({t:'action', a:{...}})` — 액션 형식은 이미 코어와 맞다(`{type:'draw',idx}` 등)
3. **자리 소유권**(함정 #8) — `sessionStorage` 우선 + `BroadcastChannel('alley_seat')` 중복 탭 방어
4. **판 종료 → 로비 전환**(함정 #3) — `m.room.phase==='lobby'` 분기를 **먼저** 둘 것.
   yut/kb/ld/lcr이 전부 이 버그를 겪었다.
5. 회귀: `npm run test:online2p`(2탭 e2e)에 3종 추가
**공용 모듈로 뺄지 검토할 것** — 지금은 6게임이 각자 인라인으로 갖고 있어 3벌을 더 만들면
같은 코드가 9벌이 된다. `public/net.js` 같은 걸로 묶는 편이 나을 수 있다.

---

## v1.235에서 한 일 (B갈래 — 카드 6종 전부 완료)

블랙잭·바카라·하이로우에 **아이템전만** 붙였다. 코어 분리는 하지 않았다 —
상대가 하우스라 온라인이 필요 없고, 코어를 빼는 건 순수 오버헤드이기 때문(1번 표의 판단 그대로).
레시피 8~9번만 적용했고 셋 다 HTML 안에서 직접 돈다(`itemsOn`/`items` 전역).

| 게임 | 아이템 | 왜 이것인가 |
|---|---|---|
| 블랙잭 | 👀 딜러 패 보기 ×2 | 이 게임의 유일한 미지수가 **딜러 뒷장**이다. 애매한 16에서 히트할지가 그걸로 갈린다 |
| 바카라 | 🔮 첫 패 엿보기 ×2 | 사람이 내리는 결정이 **'P/B/T 어디에 거나'** 하나뿐 — 그 하나를 정보에 근거하게 만든다 |
| 하이로우 | 🛟 목숨 ×2 | 긴장이 통째로 **'한 번 더 갈까, 지금 챙길까'** — 목숨을 언제 태우냐가 그 판단을 바꾼다 |

- 셋 다 **판당(또는 추측당) 1회**, 게임당 2개, 상점 판매 없음.
- ⚠️ **'AI도 쓴다' 규약은 이 3종에 적용되지 않는다** — 상대가 딜러(하우스)라
  불공정해질 상대 플레이어가 자체가 없다. CLAUDE.md 아이템전 절에도 적어뒀다.
- 하이로우 목숨은 **예약형**(좌·중·우 '칩 지키기'와 같은 방식) — 추측 전에 켜두고,
  맞히든 틀리든 그 판정에서 소모된다. 아껴 쓰려면 예약을 늦춰야 한다.

### 검증 (전부 통과)
```
npm run test:items:more   10종 통과 — 3종 절 추가
  · 블랙잭: 표시된 딜러 합이 실제 합과 일치하는지까지 단언
  · 바카라: 엿본 카드가 실제로 다음에 뽑힐 장(슈 맨 위)인지 대조
  · 하이로우: 덱을 조작해 **반드시 틀리는 추측**을 만들고 팟·연승이 지켜지는지 확인
audit-ux.js 3종           8뷰포트 × 5항목 OK (아이템 줄 추가에도 넘침 0)
test:stats 7종 · test:tutorial 13종 · test:items 2종 · check:drift 10종
```

---

## v1.234에서 한 일 (A갈래 3종 전부 완료)

### 도둑잡기 — `oldmaid-core.js` 분리
- **엔진**: `public/oldmaid-core.js` (`window.OMCore` / `module.exports`) — `OMEngine` 클래스 1개.
  최상위 `let players/hands/out/turn/phase/lastAct/busy/loser`와 `let CID`를 전부 클래스 안으로.
- **숨김 정보(레시피 2)**: **내 손패는 나만. 남의 손패는 '장수'만 — 조커가 어디 있는지가
  이 게임의 전부라 조커 위치는 절대 스냅샷에 안 싣는다.** 술래 패는 종료 후에만 공개.
- **연출을 진행에서 떼어냈다** — 옛 `initialCleanup(i)`는 setTimeout 사슬로 자리마다 짝을
  정리하면서 **게임을 진행**시켰다. 이제 엔진이 시작 짝을 한 번에 정리하고 `initialPairs`만
  남기면, 클라가 그걸로 순차 연출을 만든다. `doDraw`의 3중 중첩 타이머도 엔진의
  `pairMs`/`stepMs` 두 단계로 정리했다.
- **순서 누출을 고쳤다** — 차례 시작 때 **대상의 손패를 섞는다.** 안 섞으면 방금 건네받은
  카드가 늘 맨 끝이라 "끝자리는 쟤가 방금 받은 것"을 역산할 수 있었다(실물에서 패 든 사람이
  섞어서 부채로 펴는 이유). 원래 코드엔 이 셔플이 없었다.
- **클릭이 진짜가 됐다** — 옛 렌더는 뒷면을 5장만 그려놓고 뽑기는 `Math.random()` 인덱스라
  **누른 카드와 실제로 뽑히는 카드가 달랐다.** 이제 실제 장수만큼 그리고 `data-i`가 진짜 인덱스다.

### 아이템전 — 👀 살짝 보기 (×2)
- 뽑기 **전**에 쓰면 상대 패에서 **조커가 몇 번째인지**(없으면 '없음') 나에게만 보인다.
  도둑잡기의 긴장은 통째로 '눈 감고 한 장 집기'라, 그 한 번을 눈 뜨고 집게 하는 게 이 게임의 아이템이다.
  ('다시 뽑기'는 조커를 도로 넣는 것이라 긴장 자체를 없애서 안 골랐다.)
- AI도 쓴다(`_aiWantPeek`) — 상대 패가 적을수록 조커일 확률이 높으니 그때 쓴다(4장 이하,
  마지막 한 장은 3장 이하). 본 뒤엔 `_aiPickIdx`가 조커를 피해 뽑는다(아이템 전용 분기 없이
  사람과 같은 정보를 쓴다).

### 검증 (전부 통과)
```
npm run test:oldmaid        엔진 시뮬 2,400판 — 조커누출0·짝정리·교착없음·전원동일지급
npm run test:oldmaid:play   브라우저 완주 4조합(3·4인 × on/off)
audit-ux.js oldmaid         8뷰포트 × 5항목 OK (아이템 줄 추가에도 넘침 0)
npm run test:items:more     7종 통과 — 알려준 조커 위치가 **실제 위치와 일치**하는지까지 단언
npm run test:stats/tutorial 7종 / 13종 · npm run check:drift 10종 OK
```

### ⚠️ 남겨둔 것 — 자리 편향 (규칙 문제, 회귀 아님)
술래가 되는 확률이 자리마다 크게 다르다. **원래 HTML 로직에도 똑같이 있던 것**이라
이번 변경의 회귀가 아니다(원래 로직 독립 시뮬과 대조해 확인).

| 인원 | 자리별 술래 확률 |
|---|---|
| 3인 | 33.9 / **42.4** / 23.7 |
| 4인 | 26.8 / 23.6 / 24.8 / 24.8 (공평) |
| 5인 | 23.5 / **7.3** / 17.3 / 26.1 / 25.8 |

시작 조커 보유는 자리마다 균등하니 **딜이 아니라 턴 구조** 탓이다(먼저 뽑히는 자리가
먼저 손을 비워 세이프로 빠진다). 4인만 공평한 게 특이하다.
셋업에 3·5인 옵션이 없어(현재 3·4인만) 당장 큰 문제는 아니지만, 인원을 늘리면 먼저 봐야 한다.
고치려면 ① 매 판 시작 자리를 무작위로 돌리거나 ② 뽑는 방향을 번갈아 바꾸거나
③ 53장을 균등 분배(한 장 빼기). **재성님 결정이 필요해 손대지 않았다.**

---

## v1.233에서 한 일 (카드 6종 중 2종 완료)

### 원카드 — `onecard-core.js` 분리
1번의 9단계 추출 레시피를 그대로 따랐다. 인디언 포커와 같은 구조가 됐다.

- **엔진**: `public/onecard-core.js` (`window.OCCore` / `module.exports`) — `OCEngine` 클래스 1개.
  최상위 `let ST/players/busy/lastAct/pendingDrawn/pickJokerCard`와 `let CID`를 **전부 클래스 안으로** 넣었다.
  (`CID`는 재시작마다 값이 계속 커지던 유령 상태였다 — 이제 덱마다 0부터.)
- **숨김 정보 (레시피 2 — 먼저 한 줄로 적었다)**: **내 손패는 나만 본다. 남의 손패는 '장수'만 공개,
  내용은 아무도 못 본다. 더미는 전원 못 본다.** → 라이어·섯다와 같은 '남을 가리는' 방향이다
  (인디언 포커만 반대라 헷갈리기 쉽다). 남의 손패·더미는 **스냅샷에 싣지도 않는다.**
- **연출 분리**: 엔진은 `dealSeq`(딜) · `lastAction{seat,kind,card,amount,seq}` · `lastShield{seq,seat,blocked}`만
  남기고 **그리지 않는다.** 화면이 `prevS` ↔ `S` 차이를 보고 소리·배너를 만든다(`applyFx()`).
  기존 연출(딜·특수카드음·공격음·원카드 외침·코인비)은 하나도 안 잃었다.
- **무늬 선택은 액션에 실었다** — `{type:'play',cardId,suit}`. 엔진에 '무늬 고르는 중' 같은
  UI 하위상태를 만들지 않으려고. 반면 `pendingDrawn`(뽑은 카드를 낼지 넘길지)은 **진짜 엔진 상태**라 안에 뒀다.
- **CORE 마커** 삽입 + `build-inline.js`/`check-drift.js`에 등록 → `npm run check:drift` **9종 OK**.
- **온라인은 아직 안 붙였다** (server.js 라우팅 없음). 코어가 분리돼 #3의 사전 작업은 끝났다.

### 아이템전 — 🛡 공격 막기 (×2)
- 쌓인 공격(2·조커 누적)을 **0으로 지우고 카드를 안 먹은 채** 차례를 넘긴다. 게임당 2개.
- 왜 이 아이템인가 — 원카드에서 판을 가장 크게 뒤집는 건 **조커 맞고 +5장 먹는 순간**이다.
  그래서 "지금 +2에 쓸까, 뒤에 올지 모를 +5를 위해 아껴둘까"가 **진짜 선택**이 된다(레시피 8 기준).
- AI도 쓴다(`_aiWantShield`) — 이어치기(2·조커)로 막을 수 있으면 **아끼고**, 정말 못 막을 때만.
  마지막 한 장은 +5급에만(2단계). 시뮬 실측: 아이템전 1,200판 중 **1,055판에서 실제 사용**.

### 검증 (전부 통과)
```
npm run test:onecard  (엔진 시뮬 2,400판)             보존·누출0·교착없음·전원동일지급
브라우저 완주 6조합 (2·3·4인 × on/off)                 전부 완주 · 내 턴 5~30회 진행 확인
node scripts/browser-test/audit-ux.js onecard          8뷰포트 × 5항목 OK (아이템 줄 추가에도 넘침 0)
npm run test:items:more                                6종 통과 (원카드 절 추가)
npm run test:stats / test:tutorial / test:items        7종 / 13종 / 2종 통과
npm run check:drift                                    9종 OK
```
시뮬 단언: 카드 보존(손패+더미+버림=54) · **정보 누출 0**(스냅샷에 남의 손패·더미 없음) ·
교착 없이 항상 1명 승리로 종료 · AI가 아이템을 실제로 소진.

---

## v1.232에서 한 일 (카드 6종 중 1종 완료)

### 인디언 포커 — `indianpoker-core.js` 분리
`다음단계.md` 2번의 **8단계 추출 레시피를 그대로** 따랐다. 기존 7종과 같은 구조가 됐다.

- **엔진**: `public/indianpoker-core.js` (`window.IPCore` / `module.exports`) — `IPEngine` 클래스 1개.
  최상위 `let players/cards/pot/turn…`을 **전부 클래스 안으로** 넣었다. HTML엔 전역 상태가 없다.
- **연출 분리**: 엔진은 `dealSeq`(새 판) · `lastAction{seat,mode,amount,seq}`(칩) · `lastPeek{seq}`(아이템)만 남기고
  **그리지 않는다.** 화면이 `prevS` ↔ `S` 스냅샷 차이를 보고 딜·칩·플립·배너를 만든다(`applyFx()`).
  기존 연출(딜 애니·칩 날아가기·카드 플립·코인비·배너)은 **하나도 안 잃었다.**
- **`serialize(viewerPid)`**: ⚠️ 이 게임만 **가리는 방향이 반대**다 → 아래 '함정' 참고.
- **CORE 마커** 삽입 + `build-inline.js`/`check-drift.js`에 등록 → `npm run check:drift` **8종 OK**.
- **온라인은 아직 안 붙였다** (server.js 라우팅 없음). 하지만 코어가 분리돼 있어 #3의 사전 작업은 끝났다.

### 아이템전 — 🔍 내 카드 보기 (×2)
- 내 차례에만 · **한 판에 1회** · 게임당 2개. 쓰면 그 판 동안 **내 카드가 나에게만** 보인다.
- 원칙 유지: 아이템전에서만 · 전원 같은 개수(엔진이 지급) · 상점 판매 없음 · **AI도 쓴다**(`_aiWantPeek`).
  AI는 *돈이 걸린 뒤*(`toCall>0`)에 팟이 커졌을 때만 쓴다 — 첫 턴에 털지도, 아끼다 죽지도 않게 2단계.
- ⚠️ **레시피가 제안한 '🃏 한 장 교환'은 일부러 안 썼다.** 내 카드를 못 보는 게임에서 교환은
  기댓값이 완전히 중립이라(같은 분포에서 다시 뽑는 것) **아무 선택도 아니게 된다.** 이 게임의 유일한
  숨김정보는 '내 카드'뿐이라 거기에 손대는 아이템만 의미가 있다.

### 검증 (전부 통과)
```
node scripts/browser-test/audit-ux.js indianpoker   8뷰포트 × 5항목 전부 OK (셋업 옵션 한 줄·아이템 버튼 줄 추가에도 넘침 0)
npm run test:items:more                             5종 통과 (라이어·좌중우·섯다·알까기·인디언)
npm run test:stats                                  7종 통과 (endGame() 전역 유지 — 이 스크립트가 직접 부른다)
npm run test:tutorial                               13종 통과
npm run check:drift                                 8종 OK
```
헤드리스 엔진 시뮬(2·3·4인 × 아이템 on/off, 총 ~7,000판)로 아래를 단언했다 — 전부 통과:
칩 보존(손에 든 칩 + 팟 = 총합) · **정보 누출 0**(판 중 내 카드 null, 남의 카드 non-null) ·
교착 없이 항상 1명 생존으로 종료 · AI가 아이템을 실제로 소진.

---

## 다음에 할 일

### 1. 카드게임 코어 분리 + 아이템전 — ✅ **6/6 완료 (A·B갈래 전부)**

**6종을 똑같이 다루면 안 된다 — 온라인이 의미 있는 게임만 코어를 뺀다.**

| 갈래 | 게임 | 상태 | 왜 |
|---|---|---|---|
| A — 코어 분리 후 아이템 | 인디언 포커 | ✅ **v1.232 완료** | |
| A | 원카드(2~4인) | ✅ **v1.233 완료** | |
| A | 도둑잡기(3~4인) | ✅ **v1.234 완료** | |
| B — HTML에서 아이템만 | 블랙잭 · 바카라 · 하이로우 | ✅ **v1.235 완료** | 상대가 하우스/없음 → **온라인이 필요 없다.** 코어 분리는 순수 오버헤드 |

**이 항목은 끝났다.** 다음은 #3 온라인화다 — 코어가 다 떨어져 나와서 서버 라우팅만 붙이면 된다.

#### 추출 레시피 (v1.232·v1.233에서 두 번 써보고 다듬음)

1. **순수 함수부터 옮긴다** — `makeDeck`/`RANKS`/족보·짝 판정처럼 DOM을 안 만지는 것. 위험 0.
   **셔플·AI 난수는 반드시 주입 `rng`로** (`Math.random` 직접 호출 금지 — 5종 규약).
2. **"이 게임에서 누가 무엇을 볼 수 없나"를 한 줄로 먼저 적는다.** ← v1.232에서 새로 배운 것.
   이걸 안 적고 다른 코어를 복사하면 `serialize`가 조용히 뒤집힌다(아래 함정 1).
3. **상태를 클래스로 모은다** — 최상위 `let`을 **하나도 남기지 않는다.** 남기면 재시작 때 유령 상태가 남는다.
4. **로직에서 연출을 뜯어낸다** — 엔진은 `_emit()`만. 연출은 클라가 **seq 카운터 차이**로 만든다.
   본보기: 인디언의 `dealSeq`/`lastAction.seq`/`lastPeek.seq` → `applyFx(prevS, S)`. 윷 `lastMovePath`, kb `_lastMove`.
5. **`serialize(viewerPid)`** — 2번에 적은 문장 그대로 구현. 남의 정보는 **스냅샷에 싣지도 않는다**
   (화면에서 안 그리는 걸론 부족 — 스냅샷에 있으면 그게 곧 누출이다).
6. **UMD 래퍼**를 기존 코어에서 그대로 복사(Node `module.exports` / 브라우저 `window.XxxCore`).
7. **CORE 마커 + 스크립트 등록** — `<!-- CORE:x START -->`~`<!-- CORE:x END -->`,
   그리고 `scripts/build-inline.js`의 `MAP`과 `scripts/check-drift.js`의 `CASES` **양쪽에** 추가.
   ⚠️ 마커 두 줄을 **붙여 넣으면 정규식이 못 잡는다** — 사이에 최소 한 줄(빈 `<script></script>`)을 두고 `npm run build`.
8. **아이템전을 얹는다** — `itemsOn`/`itemCharges`/`items[]` + 액션 + `_ai…`(AI도 반드시 쓰게).
   아이템은 **그 게임의 핵심 긴장에 손대는 것**으로 고른다. 예쁜 이름보다 '선택이 되는가'가 기준.
9. **회귀 검증** — `audit:ux <game>` · `test:stats` · `test:tutorial` · `verify-items-more.js`에 절 추가.
   그리고 **헤드리스 엔진 시뮬**(수천 판)로 보존·누출·교착을 단언할 것. 브라우저 테스트로는 절대 안 잡힌다.

#### 착수 전에 읽을 것 — A갈래의 실제 비용
가장 작은 도둑잡기(506줄)조차 **게임 로직이 UI와 깊게 얽혀 있다.**
- 상태가 모듈 최상위에 흩어져 있다: `let players=[], hands=[], out=[], turn=0, phase='idle', busy, loser`
- 함수가 로직과 렌더를 같이 한다 — `discardPairs()`가 짝을 버리면서 `pairPopFX()`를 부르고,
  뽑기가 `drawFlyFX()`로 애니메이션까지 한다. `seatXY()`는 DOM rect를 읽는다.
- 인디언도 같은 상태였고, 결국 **클라 스크립트를 통째로 다시 썼다**(연출 헬퍼만 그대로 살림).
  원카드도 실제로 그랬다(클라 282줄 재작성). 도둑잡기도 같은 규모를 예상할 것.

### 2. 인디언 포커 — ✅ **완료 (v1.238)** · 판 길이 단축 + 온라인 연동

### 3. 카드게임 온라인화 — ✅ **완료 (v1.237)**
코어 분리 ✅ · 서버 라우팅 ✅ · 로비 UI ✅(공용 모듈 `net.js`).
**하우스 상대 3종(블랙잭·바카라·하이로우)은 대상이 아니다** — 사람끼리 붙는 3종만.
남은 선택지: 기존 6종을 net.js로 이관할지(위 v1.237 절 참고 — 급하지 않다).

### 4. 프리미엄 실결제 연결 — 🟡 **결정 완료 · 코드는 유입 숫자를 본 뒤 (2026-07-28)**
**📄 상세 계획은 `프리미엄_결제_계획.md`에 따로 썼다. 착수 전에 그것부터 읽을 것.**

**결정 A~D는 다 받았다**(웹 먼저 · 복구 코드 · 광고+부가혜택 둘 다 · 가격 미정) —
계획서 맨 위 '결정 완료' 절. 약관·환불 페이지도 초안이 나왔다.
**남은 막힘은 사업자등록(재성님)과 방문자 숫자**다.

⚠️ C가 '둘 다'라 **광고를 다시 켜야 한다** — `ADS_ENABLED`와 MainActivity의 `AD_INSET_DP`를
**함께** 되돌리고, 배너가 세로 52px을 먹으므로 **`npm run audit:ux`를 전 게임 재실행**할 것.
그리고 **부가 혜택(테마·스킨)은 아직 만들어진 게 없다** — 팔 물건을 만드는 작업이 따로 있다.

아래는 착수 당시 요점(그대로 유효):
- `MZ.buyPremium()`이 **무조건 `_setPremium(true)`** 다 — 콘솔 한 줄이면 공짜 프리미엄(스텁).
- **팔 물건이 없다** — 프리미엄=광고 제거인데 광고가 꺼져 있다(`ADS_ENABLED=false`).
  광고를 켜거나, 프리미엄을 다른 것으로 팔거나 **상품 결정이 먼저**다.
- **구매를 묶을 대상이 없다** — 계정도 서버 저장소도 없다(방은 메모리 Map).
  지금 붙이면 결제받고 기기 바꾸면 사라진다 → 환불 분쟁. **결제 코드보다 먼저 정할 문제.**
- 스토어(android·ios)에 올리면 **Play Billing / StoreKit 강제**라 웹 결제와 경로가 갈린다.
- 사업자등록·통신판매업 신고·약관/환불 페이지가 필요하다(지금 `privacy.html`만 있다).
  **리드타임이 기니 코드보다 먼저 착수하는 게 낫다.**

### 5. 자잘한 것 — ✅ **완료**
- ~~상점 죽은 코드~~ ✅ v1.238
- ~~셋업 길이~~ ✅ v1.239 (13종 × 8뷰포트 전부 0)

---

## v1.234에서 새로 배운 함정 (CLAUDE.md #19에 추가함)

- **엔진이 setTimeout으로 진행하면 헤드리스 시뮬이 '교착'으로 오진된다.** 동기 while 루프는
  이벤트 루프를 안 놔주므로 엔진 타이머가 영영 안 돈다 — 짝이 안 버려지고 턴이 안 넘어가
  완벽한 교착처럼 보이는데 **엔진은 멀쩡하고 하네스가 문제**였다. 실제로 첫 시뮬에서
  2,400판 전부 실패로 나와 엔진 버그로 의심했다.
  → 연출 딜레이는 `_after(ms,fn)`으로 감싸 **ms≤0이면 동기 실행**하게 두고, 시뮬은
  `pairMs:0, stepMs:0`으로 돌릴 것. 하네스를 비트는 것보다 엔진을 테스트 가능하게 만드는 쪽이 낫다.

## v1.233에서 새로 배운 함정 (CLAUDE.md #18에 추가함)

- **최상위 `let`은 `window`에 안 올라간다.** 클래식 스크립트 최상위의 `let S`는 전역 렉시컬 환경에
  들어가 `window.S`가 **undefined**다(`var`였다면 올라간다). `if(!window.S) return;`로 시작하는
  CDP 드라이버는 매 틱 즉시 빠져나가 **6조합 × 60초를 아무것도 안 한 채** 날렸다.
  화면은 멀쩡히 "내 차례"를 그리고 있어서 **게임 버그로 오진하기 딱 좋다** — 실제로 그럴 뻔했다.
  → 페이지 변수는 `typeof X !== 'undefined'`로 **맨 이름**을 참조할 것. 함정 #13의 짝이다.

## v1.232에서 배운 함정 (CLAUDE.md #14에도 추가함)

1. **`serialize(viewerPid)`가 가리는 대상은 게임마다 다르다.** 라이어·섯다는 '남'을 가리는데
   **인디언 포커는 '보는 사람 자신'을 가린다.** 다른 코어를 복사해 `s !== vseat`를 그대로 쓰면
   게임이 통째로 뒤집히는데 **화면은 멀쩡해 보인다.** → 코어를 만들기 전에 "누가 무엇을 못 보나"를 한 줄로 적을 것.
2. **CORE 마커는 START/END 사이에 최소 한 줄이 있어야 한다.** 두 줄을 붙여 넣으면
   `build-inline.js`가 "마커 없음"으로 실패한다(정규식이 `-->\n[내용]\n<!--`를 요구).
3. **테스트 하네스가 엔진을 안 놔주면 무한 루프처럼 보인다.** 시뮬에서 판이 끝날 때마다
   `setTimeout(nextHand)`을 걸어두고 `destroy()` 없이 resolve했더니, 끝난 판들이 계속 돌아
   "엔진 교착"으로 오진할 뻔했다. **엔진 버그가 아니라 하네스 버그였다.** 시뮬은 반드시 `destroy()`로 닫을 것.
4. **아이템은 '선택이 되는가'로 고른다.** 내 패를 못 보는 게임에서 '한 장 교환'은 기댓값이 중립이라
   아무 선택도 아니다. 이름이 그럴듯해도 판단이 개입하지 않으면 아이템이 아니다.
