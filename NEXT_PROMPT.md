# 다음 작업 프롬프트 — 주사위 골목

> 새 세션(VS Code의 Claude Code 등)에 아래 블록을 그대로 붙여넣어 이어가면 됩니다.
> `CLAUDE.md`가 세션 시작 시 자동 로드되므로 아키텍처·함정은 거기서 참조됩니다.

---

## 붙여넣을 프롬프트

주사위 골목(`~/yacht-dice`) 프로젝트를 이어서 작업한다. 현재 `main` 브랜치, 버전 **v1.125.0**, GitHub(`ereneren-lab/yacht-dice`) 푸시·Render 배포 완료 상태.

**⚠️ 작업 디렉토리 주의:** `~/Desktop/yacht-dice`에도 **낡은 사본(v1.84)** 이 있다. 실제 작업은 반드시 **`~/yacht-dice`** 에서 한다.

**지금까지 한 것 (v1.111~1.125):** 윷 이동연출 전면 재작성, 경로/목적지 스냅 근본수정(buildPath), 완주 게이트, 업힌 말 그룹 애니, 부스터/후퇴 잡기, 살아있는 캐릭터, 오디오 마스터 버스, 허브 메타(레벨/XP/일일목표/뱃지), 판 SVG 캐시 최적화, 잡힌 말 튕겨날기 + 리액션 이모지.
**직전 세션(v1.122~1.125):** 윷 이동 잔상, **브라우저 자동 검증 도구 신설**, 캐릭터·모션 2탄 7종(윷 특수칸 반짝임·아이들 표정·선두 왕관·던지기 긴장 / 요트 주사위 착지 juice·아바타 긴장·비차례 숨쉬기), **윷 캐릭터 카드(한게임식)** + 우측 아바타 정리.

### 🔑 이번 세션에 새로 생긴 것 — 브라우저 자동 검증 (`scripts/browser-test/`)

**이제 "브라우저가 없어 시각 검증 불가"는 옛말이다.** Playwright가 받아둔 크로미움 캐시를 `ws`로 CDP 직접 제어한다(새 의존성 0).

```bash
npm run test:fx          # 자동 단언(출발칸 잔상·reduced-motion·콘솔 예외). 서버는 알아서 띄운다
npm run capture hold     # 연출을 눈으로 → scripts/browser-test/out/*.png
npm run capture film     # 이동 구간 스크린캐스트 몽타주
npm run capture alpha    # 색·농도 후보 비교
```

**연출 작업 전 `scripts/browser-test/README.md`를 반드시 읽을 것.** 특히:
- **헤드리스는 `prefers-reduced-motion` 기본이 `reduce`** — 안 켜주면 연출이 전부 생략된 화면을 보고 "코드가 안 돈다"고 오진한다.
- **연출은 수백 ms 만에 사라진다** — 그냥 스크린샷 찍으면 못 잡는다. 복제본을 남기거나 스크린캐스트로 받아야 한다.
- **판을 축소한 몽타주로는 옅은 연출을 판정할 수 없다** — 농도 판정은 원본 해상도로.
- 게임 상태 `S`는 IIFE 스코프라 `window.S`로 못 읽는다 → DOM으로 판정.
- 윷 던지기는 `click()`이 안 먹는다 → `mousedown` → 대기 → `window`에 `mouseup`.

> 새 게임(요트·kb·ld·lcr)으로 넓히려면 `yut-drive.js`를 본떠 조작 헬퍼를 만들고 `cdp.js`는 그대로 재사용.

### ⚠️ 연출 넣을 때 반복해서 틀린 것 (직전 세션에 3번 반복)

**내가 고른 초기값이 세 번 다 "너무 옅거나 작아서 안 보이는" 값이었다.** (잔상 `α.14` → 비가시 / 밝은 갈색 `150,120,80` → 나무판과 명도 같아 비가시 / twinkle `r=1` → 비가시)
→ **새 연출은 넣고 나서 반드시 `npm run capture`로 눈으로 확인할 것.** 계측(DOM에 생성됨)만으로는 "보인다"를 증명하지 못한다.

### 이번 작업 후보 (하나 골라서 지시)

1. **요트에도 캐릭터 카드** — 직전 세션 흐름의 자연스러운 연장. 단 요트 아바타는 **이모지**라 윷(PNG 일러스트)과 접근이 다르다. 이모지를 크게 키우면 결이 달라 보일 수 있어 디자인 판단 필요.
2. **나머지 3종 폴리시** — 너클본즈(kb)·라이어(ld)·좌중우(lcr)에 살아있는 캐릭터 + 오디오 마스터 버스 적용해 윷/요트와 일관성 맞추기.
3. **온라인 랭킹 (C-2)** — server.js에 리더보드 저장/API + 허브·게임에 순위 노출. 서버·동시성 설계 필요.
4. **인게임 메타 연동** — 게임 종료 시 +XP/레벨업 연출을 5종에 추가(허브 메타를 게임 속으로).

### 반드시 지킬 규칙

- **엔진 이중구조**: 엔진 로직은 `public/*-core.js`만 고치고 `npm run build`. HTML의 `<!-- CORE:x -->` 마커 블록은 직접 수정 금지(빌드가 덮어씀). UI·렌더·CSS·연출은 마커 바깥에서 HTML 직접 수정.
- **모든 모션은 `prefers-reduced-motion`에서 자동 생략**되게(`FX_REDUCED()` 가드 또는 reduce 미디어쿼리에 selector 추가). **주의: `FX_REDUCED()`는 yut에만 있다.** 다른 게임은 `matchMedia`를 직접 쓸 것.
- **과하지 않게.** 화면이 번잡해지면 실패.
- **검증 순서:** `npm run check:drift` → 각 `<script>` 블록 `new Function()` 파싱 → jsdom 로드(5종 런타임 무에러) → `npm run test:fx` → 연출이면 `npm run capture`로 눈으로.
- **엔진 바꿨으면** `buildPath↔step`, `simStep↔step` 일치(전조합)와 100판 시뮬 0에러를 반드시 재확인.
- 응답·주석 한국어. 정확성 우선(재성은 틀리면 바로 지적함).
- 작업 단위마다 `package.json` 버전 올리고, 커밋 전 드리프트 OK 확인. 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **푸시는 배포로 이어지므로 확인받고 할 것.**

---

## 미해결 / 확인 대기

- **검증 도구가 연속 실행에 약하다** — 브라우저 프로세스가 쌓이면 `Runtime.evaluate` 타임아웃. `pkill -f chrome-headless-shell` 후 재실행하면 되지만 **회피이지 해결이 아니다.** 여유 될 때 근본 수정 여지 있음.
- **캐릭터 카드 크기 피드백 대기** — 2인 150px / 3인 120px / 4인 96px / 5~6인 72px(2열). 좌측 열이 210px라 180~200px까지는 화질이 버틴다(원본 128px PNG인데 평면 일러스트라 확대에 강함).
- 윷 잔상이 정지 화면으로는 "칸에 묻은 얼룩"처럼 보이는 면이 있음 — 재생 중엔 0.5초 안에 사라져 다르게 읽힐 것으로 보이나 실사용 피드백 필요.

## 프로젝트 빠른 참조

- 로컬 실행: `node server.js` → http://localhost:3000
- 배포: `git push` → Render 자동 빌드(`https://yacht-dice-jxva.onrender.com`) → Electron은 그 URL 로드
- 게임 파일: yut/yacht/kb/ld/lcr `.html` + 각 `*-core.js`(엔진) + `index.html`(허브)
- 상세 아키텍처·함정: `CLAUDE.md` (특히 함정 #6 애니 경로≠최종위치=스냅, #7 헤드리스 모션 검증)
- 브라우저 검증: `scripts/browser-test/README.md`
