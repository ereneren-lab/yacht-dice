# 상점 13종 표정 — 프롬프트와 순서 (2026-08-20)

무료 5종(pig·dog·sheep·cow·horse)이 **베이스 + 6표정**으로 끝났다. 남은 건 상점 13종이다.

## 먼저 알아야 할 것 셋 — 여기서 판단이 갈린다

### ① 베이스는 **다시 안 뽑아도 된다** (무료 4종 때와 다르다)

무료 4종은 pig만 8/18에 새 스타일로 가서 나란히 놓으면 혼자 달라 보였다 — 그래서 베이스까지 다시 뽑았다.
상점 13종은 눈으로 대조해 보니 **이미 pig와 같은 반질 3D 계열**이다(8/04 5종·8/11 8종 모두).
→ **표정만 뽑는다.** 그리고 이게 프롬프트를 훨씬 쉽게 만든다 ↓

### ② 레퍼런스로 **그 캐릭터 자신의 그림**을 첨부한다

무료 4종 때는 베이스를 새로 만드는 중이라 `pig.png`를 붙였다. 이번엔 베이스가 이미 정본이니
**자기 자신을 붙이는 게 가장 강한 앵커**다. 스타일뿐 아니라 뿔 개수·소품·색까지 자동으로 맞는다.

| 대상 | 붙일 파일 |
|---|---|
| 8/11 8종 (윷가락·복주머니·청사초롱·장승·해태·산신령·청룡·저승사자) | `public/img/{id}_full.png` |
| 8/04 5종 (호랑이·토끼·곰·여우·도깨비) | `public/img/{id}.png` |

### ③ ⚠️ 표정이 뜨는 게임은 **윷놀이 하나뿐이다**

`reactImg`를 부르는 파일은 `yut.html`이 유일하다. 13종을 다 뽑아도 **다른 12게임에선 안 보인다.**
78장을 뽑기 전에 이걸 알고 시작하는 게 낫다.

→ 표정의 가치를 늘리는 더 싼 길이 하나 있다: **카드게임에 승/패 표정을 배선**하는 것(코드 작업, 아트 0장).
   그러면 지금 있는 무료 5종 표정부터 12게임에서 살아난다. 아트를 더 뽑을지는 그 다음에 정해도 된다.
   (이건 제안일 뿐이고, 상점 13종을 그냥 진행해도 무방하다. 아래 프롬프트는 그대로 쓸 수 있다.)

## 뽑는 순서 — 6파(총 78장, 낱장)

> **2026-08-20 변경 — 3-업 시트를 버리고 낱장으로 갔다.** ChatGPT가 한 번에 여러 장을 잘 못 뽑는다(재성님 확인).
> 장수는 34 → 78로 늘지만 얻는 게 셋이다.
> ① 표정 하나가 캔버스를 다 써서 해상도가 좋다
> ② **얼굴이 붙어 못 자르는 사고가 아예 없어진다**(8/20에 8장 중 2장이 그랬다)
> ③ **3파가 사라진다** — 8/11 8종을 처음부터 전신으로 뽑으면 판 위 판본과 우승 화면 판본을 한 장에서 다 뽑는다
>
> **프롬프트 78개 전문: `2026-08-20_dice-alley_shop13-prompts.md`** (생성기 `scripts/gen-expression-prompts.py`)

아트가 없는 표정은 `chars.js`의 `reactImg`가 **조용히 베이스로 폴백**한다. 그래서 중간에 멈춰도 안전하다.

**표정 단위로 한 파씩** 도는 게 캐릭터 단위보다 낫다 — 1파에서 멈춰도 13종 **전부** 우승 표정을 갖는다.
캐릭터 단위로 돌면 3종만 완성되고 10종은 아무것도 없다.

| 파 | 표정 | 장수 | 왜 이 순서 |
|---|---|---|---|
| 1파 | `cheer` 환호 | 13 | 우승 화면에서 110px로 **크게** 뜨고 영구 교체된다 — 한 장당 값이 가장 크다 |
| 2파 | `star` 별눈 | 13 | 윷·모를 던질 때. 한 판에 가장 자주 뜬다 |
| 3파 | `happy` 기쁨 | 13 | 상대 말을 잡았을 때 — 윷에서 제일 신나는 순간 |
| 4파 | `surprise` 놀람 | 13 | 빽도 |
| 5파 | `sad` 슬픔 | 13 | 내 말이 잡혔을 때 |
| 6파 | `angry` 화남 | 13 | 함정(늪)에 빠졌을 때. 여기까지면 무료 5종과 동급 |

### 8/11 8종을 왜 전신으로 뽑나 — 실측한 문제

윷 결과 화면의 우승자는 **전신 그림(`_full`)을 110px쯤으로 크게** 띄운다(`yut.html:3344`).
그리고 `cheer` 아트가 있으면 그 얼굴로 **영구 교체**한다(`yut.html:3348`).

문제는 `_cheer.png`가 판 위 말(15~37px)용이라 **얼굴만 바짝 잘린 판본**이라는 것이다.
실제로 겹쳐 보니 산신령·복주머니는 **전신 → 얼굴 클로즈업**으로 확 튄다. 청룡·저승사자는 덜하다.

→ 그래서 **8/11 8종은 표정을 전부 전신 프레이밍으로 뽑는다.** 그 한 장에서
   `--bg none`으로 전신본을, `--bust --tight`로 판 위 판본을 **둘 다 뽑아낸다** — 추가 생성이 없다.
   (코드 쪽 폴백은 내가 넣는다: `_cheer_full` → `_cheer` → 베이스.)
   8/04 5종은 `full`이 없어 히어로도 잘린 판본을 쓰므로 **상반신 프레이밍 그대로**면 된다.

---

## 프롬프트 78개 — 별도 문서에 있다

**`2026-08-20_dice-alley_shop13-prompts.md`** — 복사해서 바로 쓰는 완성본 78개.
캐릭터마다 붙일 레퍼런스 파일과 저장할 파일 이름이 프롬프트 위에 적혀 있다.

그 문서는 **`scripts/gen-expression-prompts.py`가 찍어낸다. 직접 고치지 말 것** — 스크립트를 고쳐 다시 찍는다.
78개를 손으로 쓰면 공통 앞머리가 조금씩 달라지고, 그 차이가 그림의 차이로 나온다.
조각(스타일·캐릭터·표정·프레이밍·네거티브)을 조립하므로 **다른 건 표정뿐**이 보장된다.

### 그 안에 들어간 판단 — 얼굴이 없는 것들

**8/11 8종 중 넷은 사물이다**(윷가락·복주머니·청사초롱·장승). 사물에 "화난 표정, 팔짱 낀"을 시키면
모델이 **없는 팔을 만들어 내거나** 캐릭터를 사람으로 바꿔 버린다.
그래서 캐릭터마다 **무엇으로 감정을 표현할지**를 넣고, 팔이 필요한 표정(`cheer`·`angry`·`sad`)은
**표정 문장 자체를 캐릭터별로 덮어썼다**.

| id | 감정을 지는 부위 | 덮어쓴 표정 |
|---|---|---|
| `yutgarak` 윷가락 | 윷가락에 새긴 눈·입 + 두 가락이 기대는 각도 | cheer(V자로 튕겨 오름)·angry·sad |
| `bokjumeoni` 복주머니 | 금색 술 두 개가 곧 팔 | cheer(술을 번쩍)·angry(술을 팔짱처럼)·sad |
| `chorong` 청사초롱 | **안쪽 촛불** — 신나면 확 타오르고 슬프면 사그라든다 | cheer·angry·sad |
| `jangseung` 장승 | 팔도 다리도 없다. 튀어나온 눈·새긴 눈썹·붉은 입 + 기둥이 기우는 각도 | cheer·angry·sad |
| `rabbit` 토끼 | 선 귀 한쪽 | — |
| `fox` 여우 | 꼬리 | — |
| `haetae` 해태 | 구리빛 곱슬 갈기 | — |
| `sansin` 산신령 | 흰 수염·눈썹 (수염이 얼굴 반을 덮는 게 실루엣) | — |
| `yong` 청룡 | 긴 수염과 감긴 몸 | — |
| `jeoseung` 저승사자 | **작은 변화만** — 갓 기울기, 눈썹 하나, 미소 폭 | cheer(한 손만)·angry(차갑게) |
| `tiger`·`bear`·`dokkaebi` | 얼굴이 있어 그대로 | — |

**캐릭터별 추가 네거티브**도 8/11 문서 그대로 각 프롬프트에 들어가 있다
(`dokkaebi`=일본 오니 금지, `haetae`=석상 금지, `sansin`=간달프 금지, `yong`=서양 용 금지 …).
`jeoseung`에는 **배경을 검게 두지 말라**는 문장이 따로 붙는다 — 8/11에 검은 갓·도포와 안 갈려 통째로 버렸다.

---

## ⚠️ 지금까지 데인 것 (8/11·8/18·8/20 실측)

1. **배경을 검정으로 두지 말 것.** 저승사자 때 검은 갓·도포와 검은 배경을 못 갈라 통째로 버렸다.
   투명이 안 되면 중간 회색처럼 캐릭터에 없는 색으로. (`--bg none`이 알파를 그대로 믿는다.)
   → 저승사자 프롬프트엔 이 문장이 이미 박혀 있다.
2. **얼굴이 원형 크롭 밖으로 나가면 안 된다.** 판 위 말은 원형으로 잘린다 — 얼굴이 중앙 원(지름 ~88%) 안에.
3. **전신은 진짜 전신이어야 한다.** 발·받침·소품이 잘려 오면 우승 화면에서 잘린 그림을 보게 된다.
   → 프롬프트에 `nothing cropped off, centered with a small even margin`이 들어 있다.
4. ~~시트에서 얼굴이 붙으면 한 덩어리로 잡힌다~~ — **낱장으로 가서 이 함정 자체가 없어졌다.**
   (기록만 남긴다: 8/20에 8장 중 2장이 그래서, 폭이 배가 되는 덩어리 안에서 가장 성긴 열을 찾아 갈랐다.)

## 도착 후 (내가 함)

`art-src/2026-08-2X_shop13/{id}_{emotion}.png`로 넣어 주면 — **파일 이름이 곧 배선이다.**

1. `process-char-art.py --bg none` → `public/img/{id}_{emotion}.png` (400px 정사각)
   8/11 8종의 전신본은 `--bust --tight`를 한 번 더 돌려 **판 위 판본**까지 같이 뽑는다
2. 32px 원형에서 6표정이 서로 구분되는지 대조
3. `npm run test:face` 커버리지 확인(5/18 → …/18) · `check:drift` · 커밋 · 배포
4. 라이브 브라우저에서 스왑·렌더 전수 확인(무료 5종 때 30/30으로 했던 그 검사)

**코드 변경은 `_cheer_full` 폴백 한 줄뿐이다**(8/11 8종의 우승 화면용). 나머지는 이름만 맞으면 자동으로 잡힌다.

## 전제·미확정

- 전제: 상점 13종의 베이스 스타일이 pig와 붙는다고 판단했다(눈 대조). **1파의 앞 3장**
  (`jeoseung`·`chorong`·`bokjumeoni` — 8/11에 시험대로 썼던 셋)을 먼저 받아 기존 베이스와
  나란히 놓고 확인한 뒤 나머지를 뽑는 것이 안전하다. 셋이 붙으면 나머지도 붙는다.
- 미확정: 표정을 카드게임에 배선할지(위 ③). 배선하면 이 13종의 가치가 12게임으로 퍼진다.
- 미확정: 실사용자가 아직 0명이라 **어느 상점 캐릭터가 인기인지 데이터가 없다.**
  우선순위를 데이터로 정할 수 없어 위 순서는 "표정이 뜨는 빈도"로만 정했다.
