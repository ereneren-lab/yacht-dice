# 다음 단계 — 2026-07-26 세션 인수인계

이 파일은 세션이 끊겨도 이어서 작업할 수 있게 남기는 메모다.
**현재 배포본: v1.226** (전부 `origin/main`에 푸시됨 → Render 자동 빌드)

---

## 오늘(07-26) 한 일 — v1.221 → v1.226

| 버전 | 내용 |
|---|---|
| v1.221 | **공통 진행 속도(pace.js)** — 13게임 전부 AI 템포 조절(🐢느긋/🚶보통/⚡빠름). 상단바 버튼 자동 장착 |
| v1.222 | **설정 통일** — 소리·닉네임을 `alley_sound`/`alley_name`으로 모음(옛 키 자동 이관) + 탭 타깃·셋업 시작버튼 고정 |
| v1.223 | **전적(stats.js) + 통합 코인(wallet.js)** — `alley_stats`/`alley_coin`. 베팅 게임 바이인·환급, 비베팅 승리 +300 |
| v1.224 | **상점(shop.js·shop.html)** — 주사위 스킨·아바타 팩·칭호. 기존 6종도 코인/전적 합류 |
| v1.225 | 게임 내 아이템(소모품) 1차 — *v1.226에서 방식 교체됨* |
| v1.226 | **아이템전 전환 + 허브 통합 전적** — 아이템은 아이템전에서만, 전원 같은 개수 지급 |

### 새로 생긴 공용 모듈 (전 게임이 `<script src>`로 로드)
- `public/pace.js` — `PACE`/`PMS(ms)`. 진행 속도 배수. 키 `alley_pace`
- `public/stats.js` — `AS`. 전적. 키 `alley_stats` + `AS.merged()`(기존 키와 합쳐 허브에 표시)
- `public/wallet.js` — `AW`. 코인. 키 `alley_coin`(기본 100,000)
- `public/shop.js` — `SHOP`. 카탈로그·인벤토리. 키 `alley_inv`

> ⚠️ **삽입 위치 함정**: 13개 HTML 모두 `analytics.js` 스크립트가 '계측 보류' **주석 안**에 있다.
> 그 뒤에 스크립트를 넣으면 주석에 갇혀 안 뜬다(에러도 안 남). 새 공용 스크립트는 `</title>` 직후에 넣을 것.

### 새 검증 스크립트 (`npm run ...`)
```
test:pace          13게임 속도 버튼·배수
test:settings      소리/닉네임 공통 유지 + 옛 키 이관
test:stats         7종 종료 시 전적 기록·표시
test:wallet        바이인/환급/보상/이관/중도이탈/이중환급
test:shop          구매·장착이 게임·허브에 반영
test:items         아이템전에서만 뜨고, 쓰면 줄어드는지
test:items:online  호스트만 켬·꺼진 방 무시·지급 [2,2]·소진 후 차단
audit              13게임 로드/인게임 감사(에러·넘침·렉)
```
기존: `check:drift`, `build`, `test:online`, `test:online2p`, `test:seotda`, `test:fx`

---

## 다음에 할 일 (우선순위 순)

### 1. 아이템전을 다른 게임으로 확대 ← **바로 여기서 이어서**
지금은 **너클본즈(다시 굴리기)·요트(한 번 더)** 2종뿐이다. 남은 후보:
- **윷놀이**: '한 번 더 던지기' — `yut-core.js`에 이미 `useItem`(아이템 배틀)이 있으니 그 체계와 충돌하지 않게 볼 것
- **라이어 다이스**: '훔쳐보기' — 상대 주사위 1개를 1회 공개. `ld-core.js` `serialize(pid)`가 pid별로 가려주므로 거기에 1회성 공개를 얹는 방식
- **좌·중·우**: 굴림이 즉시 반영돼서 리롤이 안 맞는다 → 다른 아이템(예: 칩 1개 보호)이 필요
- **섯다**: 온라인이 없어 로컬 전용. '한 장 다시' 정도

**지켜야 할 원칙(재성님 지시)**: 아이템은 **아이템전에서만**, 그리고 **전원 같은 개수**.
상점에서 개수를 파는 건 금지(돈으로 유리해지면 안 됨). AI도 반드시 쓰게 할 것.

구현 패턴(kb/yacht와 동일하게):
1. 코어에 `opts.itemsOn` + `itemCharges`(기본 2) + `this.items[seat]`
2. 액션에서 `if(!this.itemsOn) return; if(!(this.items[seat]>0)) return; this.items[seat]--;`
3. `serialize()`에 `itemsOn`·`items` 노출 → 클라는 그 숫자로 버튼 표시
4. AI 분기에서도 같은 조건으로 소비(`_aiWants...`)
5. 서버: `lobbyPayload`에 `itemsOn` 이미 있음 / 엔진 생성 시 `itemsOn:!!room.itemsOn` 전달
6. 로컬 셋업 토글 + 온라인 호스트 토글(`setItems`)
7. `npm run build` → `check:drift` → `test:items` / `test:items:online` 확장

### 2. 프리미엄 실결제 연결
`public/monetize.js`의 `buyPremium`이 스텁이다. 상점 화면엔 '준비 중'으로만 노출 중.
지금은 광고 자체가 꺼져 있어(v1.205) 급하지 않다.

### 3. 카드게임 온라인화
블랙잭·바카라·하이로우·인디언포커·도둑잡기·원카드 6종은 **로컬/AI 전용**이다.
온라인으로 올리려면 게임별 `*-core.js` 분리 + `server.js` 라우팅 + 로비가 필요하다(기존 7종과 같은 구조).

### 4. 남은 자잘한 것
- 너클본즈 규칙 오버레이가 12px 세로로 넘친다(감사에서 ⚠️로 뜨는 유일한 항목)
- 요트·알까기 셋업 화면은 여전히 스크롤이 필요하다(시작 버튼은 고정해둠)
- 상점: 소모품 판매를 내리면서 `SHOP.count/spend`와 `buy`의 `use` 분기가 죽은 코드로 남아 있다

---

## 오늘 겪은 함정 (같은 실수 반복 금지)

1. **줄 끝 `//` 주석이 뒤 코드를 삼킨다** — v1.224에서 한 줄짜리 `Stats.record` 라인에 `// 통합 전적·코인`을 붙였더니
   같은 줄 뒤의 `this.d.games++; …`가 통째로 주석 처리돼 **5종의 자체 전적이 이틀치 안 쌓였다**. 한 줄 코드에는 `/* */`를 쓸 것.
2. **render 안의 미정의 변수는 조용히 죽는다** — 요트에서 `MYPID`(존재하지 않음)를 참조해 렌더가 중간에 끊겼다.
   에러는 안 보이고 "버튼이 안 뜬다"로만 나타난다. (CLAUDE.md 함정 #2와 동일)
3. **서버는 `require` 시점에 코어를 물고 있다** — 코어를 고쳤으면 **서버를 재시작**해야 온라인 테스트가 새 코드를 검증한다.
   (재시작 안 하고 '통과'를 본 적 있음)
4. **검증 스크립트가 없으면 로드 실패를 못 잡는다** — pace.js가 주석에 갇혀 전혀 안 떴는데 게임은 멀쩡히 돌았다.
   새 공용 스크립트를 넣을 땐 "실제로 로드됐는가"를 단언하는 테스트를 같이 만들 것.
