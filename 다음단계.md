# 다음 단계 — 2026-07-26 세션 인수인계 (v1.231)

이 파일은 세션이 끊겨도 이어서 작업할 수 있게 남기는 메모다.
**현재: v1.231** — 푸시·배포 완료. 태그 v1.228 / v1.229 / v1.230 / v1.231.

| 버전 | 내용 |
|---|---|
| v1.227 | 가로 회전 대응 · 튜토리얼 13종 · 아이템전 6종 · 허브 카테고리 필터 |
| v1.228 | 인게임 스크롤 0 — **13게임 × 8뷰포트 전부** |
| v1.229 | 셋업 길이 축소 — 세로 4폭에서 13게임 전부 0 |
| v1.230 | 가로 셋업 옵션 2열(라벨:필드 배치) |
| v1.231 | **알까기 아이템전** — 아이템전 7종 |

---

## v1.227에서 한 일

### 1. 반응형 — "스크롤 없이 플레이" + **가로 회전** 대응
새 감사 스크립트 **`npm run audit:ux`** 로 13게임 × **8개 뷰포트**(세로 5 + **가로 3**) × 5항목을 잰다.
기존 감사는 반쪽이었다 — `audit-all`은 셋업만/데스크톱만, `audit-play`는 인게임만/모바일만.

**인게임 스크롤 0 — 13게임 전부 8/8 통과** (v1.228에서 요트·섯다·알까기까지 마무리)
남은 것은 **셋업 화면 길이**뿐이다 (아래 '다음에 할 일' 참고). 시작 버튼은 13종 모두 항상 닿는다.

**근본 원인이 전부 같았다** — 높이를 확보하는 압축 규칙이 죄다 `max-width:480px`(폰 세로)에 묶여 있어
**폭이 640~844px인 가로에서는 하나도 적용되지 않았다.** 세로만 보면 0인데 폰을 돌리면 깨지는 상태였다.
→ 각 게임에 `@media (orientation:landscape) and (max-height:520px)` 블록을 만들어 같은 압축을 **높이 기준**으로 다시 걸었다.

### 2. 셋업 시작 버튼 — 진입 차단 해결
요트·섯다는 짧은 화면에서 **'시작' 버튼이 화면 밖**이라 게임을 시작조차 못 했다.
`#startLocal`에 `position:sticky`가 있었지만 **버튼이 컨테이너의 마지막 자식이라 sticky가 움직일 여유가 0**이어서 무력.
→ 짧은 화면(`max-height:700px`)에서 뷰포트 바닥에 `position:fixed`. `#setup`이 숨으면 같이 사라진다.
(알까기는 이미 `body:not(.ingame) #startRow{position:fixed}`로 되어 있었다.)

### 3. 허브 — 카테고리 필터
4분류(주사위4/전통·보드2/화투1/카드6)는 이미 있었지만 **활용이 안 됐다**(360px에서 페이지가 화면 4.9장).
→ 필터 칩 5개(`#catFilter`) 추가. 선택은 `alley_cat`에 기억, 이상값이면 전체로 복구.
→ '내 정보'(프로필·코인·전적·테마)를 **게임 카드 아래로** 이동. 필터 497→237px, 첫 카드 602→341px.

### 4. 튜토리얼 — 4종 → **13종 전부**
없던 9종(윷·요트·알까기·블랙잭·바카라·인디언포커·원카드·하이로우·도둑잡기)에 붙였다.
9개 HTML에 마크업을 복붙하지 않고 **공용 모듈 `public/tutorial.js`(`window.TUT`)** 로 만들었다.
- 마크업·CSS를 스스로 주입, 상단바에 📖 버튼 자동 장착, 첫 방문 1회 자동 오픈(`alley_tut_<game>`)
- 단계 내용은 `scripts/add-tutorials.js`에 있다(멱등 — 다시 돌려도 중복 삽입 안 됨)
- 기존 4종(kb·ld·lcr·섯다)의 자체 튜토리얼은 **그대로 유지**(중복 안 붙음)

### 5. 아이템전 — 2종 → **6종**
| 게임 | 아이템 | 비고 |
|---|---|---|
| 너클본즈 | 다시 굴리기 | 기존 |
| 요트 | 한 번 더 | 기존 |
| **윷놀이** | 방어막·재던지기·밀치기 | **기존 아이템전의 AI 공정성 버그 수정** |
| **라이어** | 👁 훔쳐보기 | 상대 주사위 1개를 나만 봄 |
| **좌·중·우** | 🛡 칩 지키기 | 굴리기 전 예약 → 나갈 칩 1개 차단 |
| **섯다** | 🃏 한 장 다시 | 로컬 전용(온라인 없음), 판마다 재지급 |

> ⚠️ **윷은 새 아이템을 추가하지 않았다.** 이미 '아이템전'이 있어서 같은 이름 두 시스템이 되면 헷갈린다.
> 대신 **`useItem`을 AI가 한 번도 호출하지 않던 버그**를 고쳤다 — 사람만 아이템을 쓰는 판이었다.
> 이제 `_aiPickItem`/`_aiUseItem`로 AI도 쓴다(밀치기=가장 앞선 상대 말, 재던지기=도 하나만 나온 턴, 방어막=잡힐 위기).

**원칙 유지**: 아이템전에서만 · 전원 같은 개수 · 상점에서 개수 판매 금지 · AI도 반드시 씀.

### 새 검증 스크립트
```
audit:ux           13게임 × 8뷰포트 × (셋업/룰오버레이/인게임/룰접근/가로잘림)
test:tutorial      13종 튜토리얼 로드·단계이동·재방문 억제
test:items:more    라이어·좌중우·섯다 아이템 (DOM 기준 단언)
```

---

## 다음에 할 일

### 1. 셋업 화면 길이 — **v1.229에서 대부분 해결**

| 게임 | 세로(360×640) | 세로 나머지 4폭 | 640×360 | 844×390 | 1024×768 |
|---|---|---|---|---|---|
| 요트 | +102 | **0** | 377 | 235 | 37 |
| 알까기 | +397 | **0** | 440 | 268 | 32 |
| 섯다 | +138 | **0** | 363 | 208 | **0** |
| 나머지 10종 | 0 | 0 | 0 | 0 | 0 |

**v1.230에서 추가로 한 것 — 대안 ②(옵션 2열) 적용.**
넓은 가로(≥700px)에서 `[라벨 | 옵션]`을 한 줄에 나란히 놓는 라벨:필드 폼 배치로 바꿨다(숨김 없음).
844×390: 요트 347→235 · 알까기 410→268 · 섯다 333→208.
⚠️ **`min-width:700px`이 필수다** — 640px 폭에서 2열로 쪼개면 칩이 더 줄바꿈해서
   알까기가 440→456px로 **오히려 나빠졌다**(실측 후 되돌림).

**한 것**
- 요트: 레벨·전적·업적·테마·위장안내(471px)를 `<details class="setup-extra">`로 **접었다**(숨긴 게 아니다).
  화면 높이 1200px 이상이면 JS가 기본으로 열어둔다. 문턱을 940px로 잡았더니 768×1024에서 열려
  오히려 +149px 넘쳤다 — 이 블록은 열리면 471px가 붙으니 문턱을 넉넉히 잡아야 한다.
- 알까기·섯다: 옵션은 **하나도 건드리지 않고** 라벨·칩 여백/글자만 줄였다(`max-height` 기준).
  알까기는 레벨바만 접었다.

**왜 360×640과 가로는 안 끝냈나** — 남은 건 전부 *판을 시작하는 데 필요한 옵션*이다.
알까기 셋업 967px 중 표면·특수·규칙 행이 각 102px, 라벨 7개가 245px이고, 섯다는 라벨 6개 + 옵션 행 5개가 전부다.
여기서 0을 만들려면 **게임 옵션을 숨겨야** 하는데 그건 취향 문제라 남겨뒀다.
가로는 높이가 360px인데 옵션만 500px이라 구조적으로 불가능하다.
**세 게임 모두 시작 버튼이 화면 하단에 고정돼 있어 스크롤과 무관하게 닿는다** — 진입은 막히지 않는다.

**더 줄이고 싶다면** 옵션을 접는 것 말고 이런 선택지가 있다:
① 프리셋 하나(예: '기본 설정으로 바로 시작') 버튼을 맨 위에 두고 나머지를 `<details>`로 접기
② 옵션을 2열 그리드로(가로에서 특히 효과적)

재보는 법: `node server.js` 띄우고 `npm run audit:ux` (특정 게임만: `node scripts/browser-test/audit-ux.js yacht seotda`)

### 2. 아이템전 남은 게임 — **알까기 완료(v1.231), 카드 6종 남음**
알까기: 🎯 '한 번 더 튕기기' ×2. 이미 있던 '잡으면 한 번 더'(extraFlick) 규칙에 얹었다 —
조준 중에 켜두면 발사 후 턴을 넘기지 않는다. AI는 돌 수가 밀릴 때 쓴다.
검사: `npm run test:items:more` (라이어·좌중우·섯다·알까기 4종)

남은 건 **카드 6종**. 다만 6종을 똑같이 다루면 안 된다 — **온라인이 의미 있는 게임만 코어를 뺀다.**

| 갈래 | 게임 | 왜 |
|---|---|---|
| **A — 코어 분리 후 아이템** | 인디언 포커(2~4인) · 원카드(2~4인) · 도둑잡기(3~4인) | 사람끼리 붙는 게임 → 온라인화(#4)의 사전 작업이 된다 |
| **B — HTML에서 아이템만** | 블랙잭(vs 하우스) · 바카라(vs 하우스) · 하이로우(혼자) | 상대가 하우스/없음 → **온라인이 필요 없다.** 코어 분리는 순수 오버헤드 |

#### ⚠️ A의 실제 비용 (2026-07-26 조사 결과 — 착수 전에 읽을 것)

가장 작은 도둑잡기(506줄)조차 **게임 로직이 UI와 깊게 얽혀 있다.** 만만하지 않다.
- 상태가 모듈 최상위에 흩어져 있다: `let players=[], hands=[], out=[], turn=0, phase='idle', busy, loser`
- 함수가 로직과 렌더를 같이 한다 — `discardPairs()`가 짝을 버리면서 `pairPopFX()`를 부르고,
  뽑기가 `drawFlyFX()`로 애니메이션까지 한다. `seatXY()`는 DOM rect를 읽는다.
- 즉 기존 7종의 '순수 엔진 + serialize 스냅샷' 구조로 만들려면 **게임당 상당한 재작성**이 필요하다.
  6종을 한 번에 하려 들지 말고 **한 게임씩** 끝내고 커밋할 것.

#### 추출 레시피 (기존 7종과 같은 구조를 만드는 순서)

1. **순수 함수부터 옮긴다** — `makeDeck` / `RANKS` / `SUITS` / 족보·짝 판정처럼 DOM을 안 만지는 것.
   이것만 옮겨도 코어의 뼈대가 생기고 위험이 없다.
2. **상태를 클래스로 모은다** — `class OldMaidEngine { constructor(opt){…} }` 안에
   `players/hands/out/turn/phase`를 넣는다. 최상위 `let`은 남기지 않는다.
3. **로직에서 연출을 뜯어낸다** — 엔진은 `_emit()`만 하고, 연출은 클라가 스냅샷 차이(`seq` 카운터)를 보고 만든다.
   윷의 `lastMovePath`/`captured`·kb의 `_lastMove`가 이미 그 패턴이다(엔진이 '무슨 일이 있었나'만 남기고 그리진 않는다).
4. **`serialize(viewerPid)`** — 남의 손패는 가리고 내 것만 보낸다. 라이어(`ld-core.js`)의 `dice: (reveal||p.pid===viewerPid)?…:null`이 본보기다.
5. **UMD 래퍼**를 기존 코어에서 그대로 복사한다(Node `module.exports` / 브라우저 `window.XxxCore`).
6. **HTML에 CORE 마커를 넣는다** — `<!-- CORE:oldmaid START -->` ~ `<!-- CORE:oldmaid END -->`,
   그리고 `scripts/build-inline.js`·`scripts/check-drift.js`의 목록에 게임을 추가한다.
   그 뒤 `npm run build && npm run check:drift`로 이중 구조를 확인한다.
7. **아이템전을 얹는다** — `itemsOn`/`itemCharges`/`items[]` + 액션 + `_ai…`(AI도 반드시 쓰게).
   카드게임은 '🃏 한 장 교환'이 자연스럽다.
8. **회귀 검증** — `npm run audit:ux <game>` (넘침·룰 접근) + `npm run test:stats`(전적 기록) +
   `npm run test:tutorial`. 아이템은 `verify-items-more.js`에 절을 추가한다(DOM 기준 단언 — 함정 7 참고).

B갈래(블랙잭·바카라·하이로우)는 6~8번만 하면 된다. HTML 안에서 `itemsOn` 플래그와 버튼만 붙이면 끝.

### 3. 프리미엄 실결제 연결
`public/monetize.js`의 `buyPremium`이 스텁. 광고 자체가 꺼져 있어(v1.205) 급하지 않다.

### 4. 카드게임 온라인화
블랙잭·바카라·하이로우·인디언포커·도둑잡기·원카드 6종은 로컬/AI 전용.
`*-core.js` 분리 + `server.js` 라우팅 + 로비가 필요하다(기존 7종과 같은 구조).

### 5. 자잘한 것
- 상점: 소모품 판매를 내리면서 `SHOP.count/spend`와 `buy`의 `use` 분기가 죽은 코드로 남아 있다

---

## 이번 세션에 새로 배운 함정 (CLAUDE.md에도 추가함)

1. **`max-width` 기반 압축은 가로 회전에서 통째로 무력해진다.** 폰을 돌리면 폭은 640~844px로 커지고
   높이가 360~390px로 줄어든다 → `max-width:480px` 블록이 전부 빠지면서 정작 높이가 필요한 순간에 압축이 사라진다.
   **높이 확보용 규칙은 `max-height`/`orientation`으로 걸 것.**
2. **`position:sticky`는 요소가 컨테이너의 마지막 자식이면 아무 일도 하지 않는다** (움직일 여유가 0).
   버튼을 고정하려면 래퍼를 고정하거나 `position:fixed`를 쓸 것. (`#helpOv .btns` 주석에 이미 있던 교훈)
3. **`body{overflow-x:hidden}`은 가로로 잘린 버튼을 숨긴다.** 넘침 수치(hOver)로는 절대 안 잡힌다 —
   너클본즈 상단바 '?'가 그렇게 사라져 있었다. `audit:ux`의 ⑤ 항목이 이걸 잡는다.
4. **HTML `style` 속성이 두 개면 뒤쪽이 무시된다.** `#rollWrap`이 `style="max-width…"` + `style="display:none"`이라
   `display:none`이 안 먹고 있었다.
5. **JS가 인라인 `display`를 넣는 요소는 CSS로 못 숨긴다** → `!important` 필요(`#kbMode`가 그랬다).
6. **CSS 미디어 블록을 기본 규칙보다 앞에 두면 특이도가 같아 기본 규칙이 이긴다.**
   `.arena{flex-direction:column}`이 뒤에 있어서 앞선 미디어의 `row`가 덮였다 — 조용히 안 먹는다.
7. **CDP 테스트에서 게임의 `S` 변수를 못 읽을 수 있다** — IIFE 안이면 `Runtime.evaluate`로 접근 불가(ld·lcr).
   **DOM으로 단언할 것**(사용자가 보는 것을 검증하는 게 더 정직하다).
8. **첫 방문 오버레이(튜토리얼·규칙)가 시작 버튼을 가려 자동 테스트가 깨진다.** 감사·테스트는 먼저 닫고 시작할 것.
