# 앱 출시 체크리스트 — Android / Google Play (2026-08-30)

> 결정: **원격 로드(코드푸시)** + **v1 광고 없음** + IAP는 나중(웹 먼저, `프리미엄_결제_계획.md`).
>   앱은 라이브 웹(github.io)을 원격 로드 → **콘텐츠·로직 업데이트는 Pages 배포로 즉시**(스토어 재심사 불필요).
>   스토어 AAB 재업로드는 **네이티브 변경 시에만**(권한·플러그인·아이콘/이름·versionCode). 오프라인은 SW 캐시.
>   (2026-09-03 결정: 코드푸시 위해 #23 원격 로드 모델로 복귀. Play는 웹뷰 내 JS/HTML OTA를 허용.)
> 목표: Play Console에 **서명된 AAB** 업로드 → 심사 → 출시.

## ✅ 코드/설정 — 이미 해둠 (이 세션)
- `capacitor.config.json`: **server.url=github.io** → 앱이 라이브 웹 원격 로드(코드푸시). 오프라인은 SW 캐시.
- `android/app/build.gradle`: `versionName "1.0.0"` / `versionCode 1`, **릴리스 서명 스캐폴드** 추가
  (`android/keystore.properties` 있으면 자동 서명, 없으면 미서명).
- `android/keystore.properties.example` + `.gitignore`(비밀은 리포에 안 들어감).
- `monetize.js`: **`ADS_ENABLED=false`** — v1은 이미 광고 OFF.
- `scripts/app-release.sh` (`npm run app:release`): 드리프트→로드모델확인→서명확인→cap sync→**bundleRelease AAB**.
- 개인정보처리방침: `public/privacy.html`·`terms.html` (URL로 제출 가능).

## 🔴 재성님만 할 수 있는 것 (순서대로)

### 1. 업로드 키스토어 만들기 (한 번, 절대 분실 금지)
```
cd android
keytool -genkey -v -keystore release.keystore -alias alley -keyalg RSA -keysize 2048 -validity 10000
```
→ `android/keystore.properties.example`를 `keystore.properties`로 복사하고 경로·비번을 채운다.
⚠️ **`release.keystore` + 비번을 잃으면 같은 앱을 영영 업데이트 못 한다.** 안전하게 백업(비밀번호 관리자 등).
(Play 앱 서명(Play App Signing)을 쓰면 구글이 배포키를 관리하고 이건 '업로드 키'가 된다 — 권장.)

### 2. AdMob 결정 (v1 광고 없음이라 둘 중 하나)
현재 `AndroidManifest.xml`에 **구글 테스트 App ID**가 박혀 있다. 광고를 안 쓰니:
- **(권장) AdMob 플러그인 제거**: `npm uninstall @capacitor-community/admob` → `npx cap sync` →
  `AndroidManifest.xml`의 `com.google.android.gms.ads.APPLICATION_ID` meta-data 줄 삭제. APK도 가벼워진다.
- 또는 **AdMob에 앱 등록**(무료) 후 그 실 App ID로 매니페스트 교체(광고 안 켜도 SDK는 합법).
⚠️ 테스트 App ID를 그대로 실출시하지 말 것.

### 3. AAB 빌드
```
npm run app:release          # → android/app/build/outputs/bundle/release/app-release.aab
```
(빌드 전 `npm run build`로 코어↔HTML 동기화 확인은 스크립트가 자동으로 한다.)
폰에서 **번들 빌드가 실제로 도는지** 먼저 확인: `npx cap sync android && cd android && ./gradlew assembleRelease` 로 APK 뽑아 설치해 봐도 된다(오프라인에서 열리는지·온라인 대전 붙는지).

### 4. Play Console (개발자 계정 $25 1회)
- 앱 만들기 → 이름 "딱세판만", 기본 언어 한국어.
- **스토어 등록정보**: 짧은 설명·긴 설명(아래 초안), 아이콘 512×512(`public/icon-512.png` 활용), 피처 그래픽 1024×500, 스크린샷(폰 최소 2장 — 실기기 캡처).
- **개인정보처리방침 URL**: `https://ereneren-lab.github.io/yacht-dice/privacy.html`
- **콘텐츠 등급** 설문(도박 요소 주의: 섯다·블랙잭 등 '가상 도박' 항목 정직히 답 — 실제 화폐 없음).
- **데이터 보안** 양식: 수집 데이터 없음/광고 없음/계정 없음에 맞춰 작성(GoatCounter는 쿠키·개인식별 없음).
- **대상 연령**, 앱 콘텐츠 설문 완료.
- 프로덕션 트랙에 **AAB 업로드** → 검토 제출.

## 스토어 설명 초안 (복붙용)
**짧은 설명(80자)**: 설치 없이 링크 하나로 친구와 즐기는 13가지 보드·주사위·카드게임.
**긴 설명**:
> 커피 한 잔 걸고 딱 세 판. 윷놀이·요트 다이스·너클본즈·라이어 다이스·좌·중·우·알까기·섯다·인디언 포커·원카드·도둑잡기·블랙잭·바카라·하이로우 — 13가지를 한 앱에서.
>
> · 혼자(AI)로도, 친구와 온라인으로도 · 방 만들고 링크만 보내면 바로 시작 · 계정·설치 강요 없음
> · 한국식 캐릭터 18종과 표정 연출 · 가상 칩으로 즐기는 캐주얼 게임(실제 도박 아님)

## ⚠️ 심사에서 주의
- **도박 표현**: 섯다·블랙잭 등은 **가상 칩**이며 실제 화폐 결제·환전이 없음을 콘텐츠 등급·설명에 명확히. (실화폐 도박으로 오인되면 반려)
- **최소 기능**: 번들이라 "웹뷰 껍데기" 반려 위험은 낮지만, 스크린샷·설명으로 '게임 앱'임을 분명히.
- **타깃 SDK**: Play 최신 정책의 targetSdk 요건 충족 확인(`variables.gradle`).

## 나중 (v1 이후)
- 수익화: 웹 프리미엄(광고 제거+혜택) → 이후 Play Billing IAP (`프리미엄_결제_계획.md`).
- 콘텐츠 자동 업데이트: 정식 OTA(@capgo) 도입 시 번들+OTA 병행.
- iOS: `capacitor.config`에 ios 설정은 있으나 별도 심사·계약 필요.
